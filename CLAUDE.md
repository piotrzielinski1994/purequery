# DbUI

Briefing for Claude Code. Read [README.md](README.md) first - setup, commands, repo layout. This file lists conventions and the non-obvious bits not visible from reading individual files.

## Communication

- Keep replies short and to the point. No filler, no pleasantries, no recap of what the user just said.
- Status updates fit in one or two sentences.

## UI rules (non-negotiable)

- **Native browser context menu is suppressed app-wide** - `installBrowserDefaultGuards(window)` ([src/lib/browser-defaults.ts](src/lib/browser-defaults.ts)) mounted once in [src/app/providers.tsx](src/app/providers.tsx) preventDefaults `contextmenu` (a desktop app must never show Reload/Inspect Element). The app's own radix `ContextMenu`s are unaffected - they draw their menu + preventDefault on the trigger before this window bubble-phase guard runs, so it only swallows the native menu where no app menu handled the right-click. (Ported from `vidui`'s browser-defaults; the reserved-keyboard-shortcut slice was NOT ported - context-menu only.)
- **A database connects when its sidebar row is EXPANDED, not only when its card is active.** `DatabaseRow` ([src/components/workspace/tree-row.tsx](src/components/workspace/tree-row.tsx)) has a mount effect that auto-connects an expanded+idle database (fires once per mount via a ref), so a database restored expanded on launch (persisted `expandedIds`) populates its tables instead of showing a down-chevron with no children until a manual collapse+expand. The chevron toggle still connects on user-expand; the database card's `useAutoConnect` still connects the active tab. Because an expanded row that is ALSO the active tab now has two connect triggers on the same mount tick, `connect()` in [use-connection.ts](src/components/workspace/use-connection.ts) dedups via a module-level `inFlightConnects` Set (the `connectionStatus === "connecting"` guard is racy on mount - both read `idle` before setState lands). Tests reset it via `__resetInFlightConnects()` in `beforeEach` (a never-resolving connect mock would otherwise leak an id and drop the next test's connect).
- **All visual/interaction rules live in [docs/design.md](docs/design.md)** - read it before any UI change. Highlights, all enforced: NO rounded corners anywhere (`--radius` pinned to `0rem` in [src/index.css](src/index.css)); dividers are 1px and NEVER thicken/colour on hover or drag (use an invisible `::after` hit area); compact IDE density; theme tokens not hard-coded colors. The user has flagged rounded corners and thick borders repeatedly - treat either as a defect.
- **Sidebar tree drag-and-drop** (reparent databases/folders into folders, reorder siblings) mirrors the `requi` repo's `@dnd-kit/core` tree. Pure logic: [src/lib/workspace/tree-edit.ts](src/lib/workspace/tree-edit.ts) (`findNode`/`containsId`/`removeNode`/`insertNode`), [src/lib/workspace/move.ts](src/lib/workspace/move.ts) (`moveNode` + folder-parent/cycle guards), [src/lib/workspace/tree-locate.ts](src/lib/workspace/tree-locate.ts) (`projectDropPosition`/`dropTarget`/empty-zone ids); drag-state context is [src/components/workspace/tree-dnd.tsx](src/components/workspace/tree-dnd.tsx). **Only `folder` and `database` rows are draggable; `table` leaves are NOT** (ephemeral live-catalog nodes, never persisted-movable - so `moveNode` rejects a database/table parent, and `TableRow` has no drag wiring). A move flows through `WorkspaceProvider.moveNode` -> `setTree` -> the existing `onTreeChange` persist effect (no new wiring). dnd-kit pointer drags don't run in jsdom, so behavioral ACs live in the pure-lib tests; inject a crafted `TreeDndProvider` state to test cue rendering.
- **Sidebar multi-select** (Cmd/Ctrl+click toggle, Shift+click range, plain click resets) lives in `WorkspaceProvider`: `selectedIds: Set<string>` + `selectInTree(id, mode)` + `clearSelection`, with range computed over the VISIBLE rows by [src/lib/workspace/tree-select.ts](src/lib/workspace/tree-select.ts) (`flattenSelectable`/`rangeBetween`). Only folders/databases are selectable (tables excluded). Backspace/Delete (macOS delete emits Backspace) on a non-empty selection opens the shared `DeleteNodeDialog` (now takes `nodes: TreeNode[]` and renders "Delete N items?" for a multi-delete); the global window-keydown listener in [src/components/workspace/sidebar-tree.tsx](src/components/workspace/sidebar-tree.tsx) is guarded by `isEditableTarget` ([src/lib/workspace/is-editable-target.ts](src/lib/workspace/is-editable-target.ts)) so typing in an input/CodeMirror never deletes. Bulk delete flows through `removeNodes(ids)` (the single `removeNode` delegates to it). A right-click Delete on a row inside the selection deletes the whole selection.
- **Multi-select + drag** moves the WHOLE selection: dragging a row that is part of a multi-selection calls `moveNodes(ids, target)` ([src/lib/workspace/move.ts](src/lib/workspace/move.ts)); dragging an unselected row stays single (`moveNode`). `moveNodes` drops selected descendants of a selected folder (the folder carries them), inserts in tree document order, and rejects dropping into a dragged folder. It wants the RAW drop index, so `handleDragEnd` uses `rawDropTarget` (no single-node compensation) for the multi path while the single path keeps `dropTarget`'s compensation - `dropTarget` is now `rawDropTarget` + the single-node shift.
- **Sidebar/tab context menus** (ported from requi): the empty sidebar area ([src/components/workspace/sidebar-tree.tsx](src/components/workspace/sidebar-tree.tsx), `ContextMenu` wrapping the `<ul>`) -> New database / New folder at root; a folder row -> New database / New folder INSIDE it + Rename + Delete; a database row -> Connect/Disconnect + Rename + Delete (Disconnect also COLLAPSES the row - a disconnected database renders no table children, so leaving it expanded is misleading and re-expanding to reconnect would need a redundant collapse); an open content tab ([src/components/workspace/content-header.tsx](src/components/workspace/content-header.tsx)) -> Close / Close other tabs / Close all. A row's own (nested) `ContextMenuTrigger` wins over the empty-area menu, so right-clicking a row shows the row menu, not the root one. Context actions live in `WorkspaceProvider`: `addDatabase(parentId?)` / `createFolder(parentId?)` (insert inside the folder, auto-expand it; `createFolder` then opens the inline rename), `renameNode(id, name)` (renames ANY node, ignores blank), `renamingNodeId`/`beginRename`/`cancelRename`, `closeOtherTabs(keepId)`. Inline rename is the requi `RenameInput` in [src/components/workspace/tree-row.tsx](src/components/workspace/tree-row.tsx) (Enter commits, Escape cancels, blur commits, with the readyRef guard against the radix focus-teardown blur; `onPointerDown` stops the drag from starting while editing). The separate `NewFolderDialog` + `addFolder(name)` (Cmd/Ctrl+Shift+N, command palette) still exist for the root-folder path.
- **Grid row multi-select**: the shared `DataGrid` takes `selectedRows: Set<number>` + `onSelectRow(index, mode)` (NOT a single `selectedRow`) - Cmd/Ctrl+click toggles, Shift+click ranges, plain click replaces, via the pure reducer [src/lib/workspace/row-select.ts](src/lib/workspace/row-select.ts) (`nextRowSelection`). The editable table card ([table-card.tsx](src/components/workspace/table-card.tsx)) holds the selection STAMPED with the `rows` array it was made against - when `rows` change (sort/filter/paging/refetch) the stamp mismatches and the selection reads empty, so a positional bulk delete never hits the wrong rows (no reset-effect; `set-state-in-effect` is a lint error here). Bulk delete = `onDeleteRows(indices)` -> stages one delete mutation per row (reversible via Changes tab, same as single delete); shown as "Delete N rows" in the row menu and bound to Delete/Backspace (guarded by `isEditableTarget` + the grid must contain focus). The read-only SQL result grid passes a stable empty Set + no `onDeleteRows`, so it's non-selectable. RecordView focuses `selection.anchor`.
- **Table filter lives in the provider, keyed by tableId; the filter editor is remounted per table.** The APPLIED filter is `WorkspaceProvider.tableFilters: Map<tableId, string>` + `setTableFilter(tableId, filter)` (in-memory, NOT persisted) - the `TableCard` is a singleton keyed on the active node, so a content-tab switch unmounts it; keeping the filter local (as it once was) lost it on every switch. The card's editor DRAFT (`filterText`) stays local but re-seeds from the applied value via adjust-state-on-render when `tableId` changes, and the `SqlEditor` carries `key={tableId}` so its CodeMirror doc remounts fresh per table (one table's filter never lingers in another's editor). Regression tests in `table-content.test.tsx` ("filter persistence across tab switch").
- **Refresh re-fetches the open table** ([table-card.tsx](src/components/workspace/table-card.tsx)): a `RefreshCw` button in the filter bar (live tables only) + the grid-scope `refresh-table` shortcut (default `Mod+R`, `preventDefault` so it never reloads the webview, `isEditableTarget`-guarded) invalidate the `["table-rows", tableId]` / `["table-count", tableId]` / `["table-structure", tableId]` react-query keys, so LiveTable's queries refetch (a fresh SELECT appears in History, like a sort/Load-more). Keyed by tableId, so only the active table refreshes.
- **ONE data grid, always identical.** The table card and the SQL result pane MUST render rows/cells/headers with the exact same component - [src/components/workspace/data-grid.tsx](src/components/workspace/data-grid.tsx) (`DataGrid`). Never fork a second grid or diverge their styling. Read-only callers (SQL results) pass `editable={false}` + no-op edit handlers; the editable table card passes the real ones. If a grid change is needed, change `DataGrid` so both update together. (Grid visual rules are in design.md.) **MongoDB documents render through this SAME grid** - the backend flattens documents to the shared `TableRows` shape (top-level keys -> columns, `_id` first + `PK`, nested object/array -> compact JSON text, missing field -> `[NULL]`). Do NOT build a document-specific view.

- **JSON view** (third table view, alongside grid + record view): toggles an editable, foldable JSON array of the loaded rows over the grid. Pure logic is [src/lib/workspace/json-edit.ts](src/lib/workspace/json-edit.ts) - `rowsToJson`/`parseJsonRows` (ADT) + `diffToMutations` (diffs the edited array against the saved rows BY PRIMARY KEY into rowIndex-keyed intents). The view component is [src/components/workspace/json-view.tsx](src/components/workspace/json-view.tsx) (reuses the `sql-editor-theme` CodeMirror chrome + `@codemirror/lang-json` fold; read-only when given no `onSave`). The toggle is an INDEPENDENT boolean lifted to `WorkspaceProvider` (`isJsonView`/`toggleJsonView`, like `isSidebarVisible`) so the palette command and the grid-scope `toggle-json-view` shortcut (`Mod+Shift+J`) share it; record view stays local to `TableView`. **It does NOT fork the grid** (respects the "ONE data grid" rule) - it's an alternate view of the same rows. Edits AUTO-STAGE on a debounce into the EXISTING pending-edits pipeline (NO local Save/Discard - the Changes-tab pending bar is the single stage/commit gate; its Save shows the SQL before touching the DB, so the mass-delete footgun is bounded by that explicit commit, not the JSON buffer): SQL change -> `cell` per field (SQL has no whole-row `Replace` - rejected by `build_mutation`), Mongo change -> `replace`, new PK -> `insert`, missing PK -> `delete`. `saveJson` in `LiveTable` is RECONCILING + idempotent (debounce calls it on every edit): it builds each mutation's id via `jsonMutationId` (pure, in json-edit.ts - cell/delete/replace key by row identity, insert keys by PK value so a re-diff of the same new row collides), re-stages the current diff, and discards any id it previously staged that the new diff dropped (tracked in `jsonStagedIds` ref), so reverting an edit un-stages it and a re-fire never duplicates an insert. Diff rejects (stages nothing): bad JSON, SQL unknown/missing column key, duplicate PK. Draft (unsaved-insert) rows are excluded - the JSON view diffs SAVED `rows` only (`jsonRows` prop), drafts stay in the grid. **The change-diff compares CANONICAL values, not raw strings** (`canonicalCell` re-serialises the stored cell the way an edit would): a Postgres `jsonb` cell comes back as `{"vip": false, "seat": 1}` (spaces) but an edit re-emits `{"vip":false,"seat":1}` (compact) - a raw string compare flags EVERY structured row as changed (one spurious `cell` per jsonb column per row). `JsonView` is just the CodeMirror editor (NO buttons, NO persistent status bar - an error bar appears below it ONLY on bad-JSON/validation failure); it fills height directly (own scroll) and is NOT wrapped in the grid's `ScrollArea` (that grew the editor to full content height) - the `ScrollArea` lives inside `TableView` around the grid/record branches only.

- **Read-only connection mode** (F11 - prod safety): a per-database `readOnly: boolean` flag ON the database node (default false), edited via the `ReadOnlyField` square switch in the Settings tab (under accent color) and persisted in `workspace.json` (mirrors `accentColor` - `mergeReadOnly` keeps only boolean `true`, `hydrate` defaults missing to false, `dehydrate` OMITS false). Provider action `setDatabaseReadOnly(id, ro)` mirrors `setDatabaseAccent` exactly. When ON it blocks EVERY frontend write path at the boundary - **no backend change**: (1) table mutations fold into the ONE existing `editable` gate in [table-card.tsx](src/components/workspace/table-card.tsx) - `editable = primaryKey !== null && !readOnly` - so inline edit / Add-row / delete / bulk-delete / clone / Mongo doc-edit / JSON-view auto-stage all disable together via the SAME no-PK read-only rendering (no new disabled state), and the table Save bar is hidden (`save()` also early-returns); (2) a write-shaped command in [sql-tab.tsx](src/components/workspace/sql-tab.tsx) `submit` is rejected before `run.mutate` with a sticky warning toast + a History error line, using the pure guard picked by engine ([dispatch.ts](src/lib/script/dispatch.ts)): `isWriteSql` (leading SQL keyword, the SAME guard the Script tab uses) for the SQL engines, `isWriteMongo` (the `.op(` after `db.<coll>.` matched against `MONGO_WRITE_OPS`) for MongoDB - because the Mongo Query tab now runs writes too (updateOne/insertOne/...), so it is NO longer read-only-by-default at the backend. There is NO other frontend DB-send path (Changes tab has Discard only, no global Save). Prefix-only like the Script tab: a read-led multi-statement buffer chaining a write is a known, documented gap. When adding a NEW write path, gate it on the owning node's `readOnly` the same way.

- **Structure view** (F6 #14 - fourth table view): a READ-ONLY metadata panel over the grid showing the open table's Columns (name/type/nullable/PK/default/ordinal), Indexes (name/fields/unique/primary), Foreign keys, and Constraints (check+unique). Component is [src/components/workspace/structure-view.tsx](src/components/workspace/structure-view.tsx); it is NOT the shared `DataGrid` (metadata, not editable rows - so the "ONE data grid" rule doesn't apply, it's an intentionally separate `<table>`). Toggle is an INDEPENDENT boolean in its OWN `StructureViewContext` (`useStructureView`/`isStructureView`/`toggleStructureView`, mirroring `useJsonView`'s perf isolation - NOT the workspace value, so it never churns the heavy `TableCard`); shared by the palette command + grid-scope `toggle-structure-view` shortcut (`Mod+Shift+I`). **The keyboard listener lives in `LiveTable`, NOT `TableView`** - opening the view unmounts `TableView` (StructureBranch replaces it), so a listener there would make the shortcut one-way (bug caught in review; regression test in `structure-toggle.test.tsx`). Data is fetched via `fetchTableStructure` react-query (`staleTime: Infinity`, keyed `["table-structure", tableId]`); only LIVE (connected) tables have the view - the static/mock path never reads `isStructureView`. **The `enabled` gate is `isStructureView || !isMongo`**: a SQL table fetches its structure EAGERLY (not gated on the view) because FK navigation (F13) needs the foreign keys before the Structure view is ever opened - the FK header marker + "Go to" row menu draw on first render; Mongo stays lazy (no FKs, nothing needs it early). `StructureBranch` renders loading/error/data; each empty section shows "None". MongoDB shows ONLY the Indexes section (documents have no columns/FK/constraints).

- **FK navigation** (F13 - SQL only): a live SQL table row's right-click menu shows a "Go to `<refTable>` (`col=value`)" item per outbound foreign key whose local column value(s) are all non-null; selecting it `openNode`s the referenced table's tab + `setTableFilter`s a WHERE fragment pinning the referenced row (`<refCol> = <value>` AND-joined). FK local columns are marked `FK` in the grid header (alongside `PK`/`NN`), and an FK cell's value renders as a **Cmd/Ctrl+click link** (dotted underline, `--primary`) that fires the SAME navigation - `foreignKeyForColumn`/`isForeignKeyNavigable` ([foreign-key-nav.ts](src/lib/workspace/foreign-key-nav.ts)) pick the fk per cell; the modified click `stopPropagation`s so it never also toggles the row selection, a plain click selects the row like any cell (visual rule in design.md). Pure logic: [src/lib/workspace/foreign-key-nav.ts](src/lib/workspace/foreign-key-nav.ts) (`navigableForeignKeys` = non-null-value FKs + label; `fkTargetTableId` = the `${databaseId}::${referencedSchema ?? ""}::${referencedTable}` node-id formula) + `fkFilter` in [query-preview.ts](src/components/workspace/query-preview.ts) (engine-quoted idents + `sqlLiteral`-escaped values). The `DataGrid` takes `foreignKeys` + `onFollowForeignKey`; `LiveTable` owns `followForeignKey` (resolves the target id, `toast.error` if not in `nodesById`, else `navigateTo` - NO `readOnly` gate, navigation is a read). A row can carry MULTIPLE FKs to DIFFERENT tables (one "Go to" item each); a COMPOSITE FK (>1 column) is ONE item joining every `refCol=value` pair. **The PG FK introspection query correlates the referenced column by `position_in_unique_constraint` against `key_column_usage` (NOT `constraint_column_usage`)** - the latter joins only on constraint_name and fans a composite FK into a cartesian product (2-col FK -> 4 rows -> duplicated pairs in the label); `db.rs`'s `foreign_key_query` test pins this. **Requires `referencedSchema` on `ForeignKey`** (Rust `db.rs` + TS `model.ts`): the PG FK query selects `ccu.table_schema`, MySQL/SQLite select `NULL`, so a cross-schema Postgres FK resolves to the correct node. Mongo has no FKs so no items/marker appear.
  - **Back/forward navigation**: FK jumps go through `WorkspaceProvider.navigateTo({tableId, filter})`, which pushes onto a `(tableId, filter)` history stack (pure reducer [src/lib/workspace/nav-history.ts](src/lib/workspace/nav-history.ts) - `pushNavigation` seeds the source as the base entry so Back returns to it, drops forward history on a new jump, no-ops a re-jump to the current position). `goBack`/`goForward` walk the cursor, restoring BOTH the active tab (`openNode`) and that table's filter (`setTableFilter`). Exposed as `canGoBack`/`canGoForward` + Back/Forward arrow buttons in the content header (`leading` slot of `TabBar`) + `nav-back`/`nav-forward` shortcuts (`Mod+[` / `Mod+]`, global scope). In-memory only. (This replaced the original "rely on the tab bar" decision - see adr.md.)

- **Mock data generator** (F17): a "Generate mock data" palette command (Create group, gated on `isTableActive`) opens the `MockDataDialog` ([src/components/workspace/mock-data-dialog.tsx](src/components/workspace/mock-data-dialog.tsx)) over the open live table. Pure logic is [src/lib/workspace/mock-data.ts](src/lib/workspace/mock-data.ts): `autoStrategy(column)` (type/name/PK -> a default `MockStrategyKind`) + `generateRows(configs, count, seed)` (a **seeded deterministic** mulberry32 PRNG -> ADT `Result`; validates count against `MAX_MOCK_ROWS=200` + rejects an empty enum list; a `skip` column is OMITTED from each row object, a `null` column present-as-null - the two are DISTINCT). The dialog offers a per-column strategy picker (auto-defaulted) + params + a row count/seed, previews via the shared read-only `DataGrid` (ONE-grid rule), and on Insert calls back to `LiveTable.stageMockInserts`, which stages one draft `insert` pending edit per row (SAME shape as `addRow`/`cloneRow` - `preview.insert` sql, reversible via the Changes tab, commits through the one table Save gate) - **no backend change**, no new mutation kind. Columns come from the fetched `data.pages[0].columns` (uniform SQL+Mongo metadata), so no engine branch and no extra structure fetch; the dialog is rendered only by `LiveTable`, so a static/mock table never mounts it. Gated on the SAME `editable = primaryKey !== null && !readOnly` term (F11) - a read-only DB or PK-less table disables Insert with a note. Open flag lives in an isolated `MockDataContext` (`useMockData`/`isMockDataOpen`/`openMockData`/`closeMockData` + an `initialMockDataOpen` provider seed), mirroring `StructureViewContext`'s perf isolation so opening the dialog never churns the heavy `TableCard`. No `faker` dep (hand-rolled word/name lists). SQL + MongoDB (Mongo `_id` auto-defaults to `skip`).

- **Session Logs tab** (F18 - bottom panel, fourth tab): the `Console` component ([src/components/workspace/console.tsx](src/components/workspace/console.tsx)) gains a **Logs** tab showing the application file-log lines for the current launch (connect/disconnect/query/mutations + the FE `log_message` bridge - the SAME lines written to `dbui-<ts>.log`). **DISTINCT from the existing "Console" tab** (internal id `"log"`, `consoleLines` = script `console.log`/`print` output); Logs is the app log, Console is script output. Lines are PUSHED, not read from the file: `logging::init` adds `TargetKind::Webview` to its `.targets([...])` (the ONE backend change - additive, the file still gets each line once), so tauri-plugin-log forwards every `log::` record to the webview as a `log://log` event. The FE subscribes via a `LogStream` port ([src/lib/logging/log-stream.ts](src/lib/logging/log-stream.ts) - `createTauriLogStream` wraps the plugin's `attachLogger`, `createNoopLogStream` for browser/jsdom, picked by `isTauri()` in [src/routes/index.tsx](src/routes/index.tsx) and injected as the `logStream` prop, mirroring `WindowController`). Lines land in an ISOLATED `LogLinesContext` (`useLogLines`/`logLines`/`appendLogLine`/`clearLogLines` in [workspace-context.tsx](src/components/workspace/workspace-context.tsx), mirroring `MockDataContext`/`StructureViewContext` perf isolation - a log append must NOT rebuild the heavy workspace value/`TableCard`). Pure logic: `parseLogLine(raw, pluginLevel?)` ([src/lib/workspace/log-line.ts](src/lib/workspace/log-line.ts)) splits `[ts][LEVEL] msg` into `{raw, timestamp, level, message, kv}` (level from the numeric plugin level 1..5 first, `[LEVEL]` token fallback; `kv` scans `key=value` so a space-bearing error tail stays in `message`, never throws) + `filterLogLines(lines, query)` ([src/lib/workspace/log-search.ts](src/lib/workspace/log-search.ts)) - structured `field:value` search (fields `level`/`message` + kv keys; quoted values, unknown field -> bare term on `raw`, case-insensitive substring, AND-combined). Coloring is level+syntax (whole-line tint by level + muted timestamp + level badge + dimmed kv keys/accented values). **Live-only** (documented gap): lines emitted before the FE listener mounts (`dbui starting`, launch-time auto-connect) are not shown - no file read-back command. NO rising-edge auto-focus (unlike History/Changes/Console - a passive tab). No capability change needed (`attachLogger`'s `listen('log://log')` is covered by `core:event:allow-listen` in `core:default`). Requires the `@tauri-apps/plugin-log` JS dep.

- **JS scripting / Script tab** (F7 - read-only): the database-card **Script** tab runs user JavaScript in an isolated **Web Worker** behind a `ScriptRunner` port ([src/lib/script/runner.ts](src/lib/script/runner.ts) - `createWorkerRunner` real, `createNoopRunner` for jsdom; `ScriptTab` takes an INJECTED `runner` prop so tests pass a fake, mirroring `WindowController`). jsdom has no real Worker, so all behavioral tests inject a fake runner and drive its `onLog`/`onRpc`/`onDone`/`onError` handlers. The worker ([src/lib/script/worker.ts](src/lib/script/worker.ts), a Vite `?worker` module) wraps the code in an `AsyncFunction` with `db`/`console`/`print` injected; it CANNOT call Tauri `invoke` - every `db.*` call posts a `{kind:"rpc"}` message and awaits a reply. The main-thread `handleRpc` in [script-tab.tsx](src/components/workspace/script-tab.tsx) maps each method to an EXISTING command (`executeSql`/`executeMongo`/`fetchSchema`) - **no backend change, no new Tauri command**. **READ-ONLY is enforced on the bridge**: there are no write methods, and a write-shaped SQL passed to `db.query` is rejected by the pure `isWriteSql` guard ([src/lib/script/dispatch.ts](src/lib/script/dispatch.ts), leading-keyword after comment-strip, best-effort/prefix-only) with a Console error line + a sticky (`duration: Infinity`) warning toast - it never reaches `executeSql`. `db.query` maps the backend `QueryOutcome` (columns + string[][]) to an **array-of-objects** (`r.name`); Mongo `db.find`/`db.aggregate` build the full `db.<coll>.<op>(<json>)` command string the backend `parse_command` expects (pass args[0]=collection + args[1]=filter/pipeline - do NOT pass the bare collection name, the parser needs the `db.` prefix). Output: `console.*`/`print` stream LIVE to the bottom **Console** panel via `appendConsoleLine` (the log now APPENDS across runs and is state, seeded from the `consoleLines` prop; only the Console **Clear** button wipes it - `[error]`-prefixed lines render red, and a new line rising-edge focuses the log tab). A **result grid renders ONLY** when the return passes the pure `parseGridReturn` ([src/lib/script/result.ts](src/lib/script/result.ts), `{header:string[], rows:(string|null)[][]}`, ragged rows rejected); it reuses the shared read-only `DataGrid` (ONE-grid rule) - an ill-shaped `{header,rows}` logs a validation error and renders no grid. Scripts persist as their OWN document tabs: `savedJsScripts: SavedJsScript[]` (`{name, code}`) on the database node (SEPARATE from the SQL `savedScripts`; the dead `script: string` field was removed) with a JS-suffixed mirror of the SQL script provider actions (`saveJsScript`/`updateJsScript`/`renameJsScript`/`deleteJsScript`, `activeJsScriptByDb`/`jsBuffers`). The editor is a small `JsEditor` ([src/components/workspace/js-editor.tsx](src/components/workspace/js-editor.tsx)) - reuses the SQL editor's theme + run/save keymap bridge, uses `@codemirror/lang-javascript`, and has its OWN completion source (`scriptCompletionSource`, engine-aware): a bare identifier -> the injected globals (`db`/`console`/`print`/`await`); `db.` -> the engine's read methods (SQL `query`/`tables`/`schema`, Mongo `find`/`aggregate`/`collections`/`schema`); `console.` -> `log`/`error`; and inside a `db.query("`/`db.find("`/`db.aggregate("` string literal -> the schema's table names (SQL) / collection names (Mongo). It owns its CodeMirror scroller (NOT wrapped in a bare overflow/`ScrollArea`, like the JSON view). The Script tab is on ALL engines incl. MongoDB (added to `MONGO_SECTIONS`). Run/Cancel mirrors the SQL tab (`run-query` shortcut; Run flips to Cancel = `runner.terminate()`, neutral `Cancelled` status); a run over ~5s raises a sticky warning toast (no auto-kill). **A backend error from a `db.*` call must be RETURNED as an error reply, never thrown out of `handleRpc`** (and `createWorkerRunner` `.catch`es a rejected `onRpc` too) - a rejected reply the worker awaits would hang the run forever (deadlock). The Script tab uses the SAME `HorizontalSplit` editor|results layout as the SQL tab (its own `layouts.script` panel-group key + the shared `splitOrientation`), so the `toggle-split-orientation` command/`Cmd/Ctrl+\` works there - `isSplitView` (in `command-palette.tsx` + `workspace-layout.tsx`) is true for the `sql`/`query`/`script` database tabs, not `sql` alone.

- **Window fullscreen persistence** (ported from `requi`): the native window's fullscreen state is remembered across launches. Behind a `WindowController` port ([src/lib/window/window-controller.ts](src/lib/window/window-controller.ts) - real Tauri window vs a noop for browser/jsdom, picked by `isTauri()` in [src/routes/__root.tsx](src/routes/__root.tsx)) so the sync hook is unit-testable without a webview. `useWindowFullscreenSync` ([src/lib/window/use-window-fullscreen-sync.ts](src/lib/window/use-window-fullscreen-sync.ts)) is mount-once two-way: restore a saved-true fullscreen on launch (a saved-false NEVER forces windowed - don't fight an OS-opened fullscreen), then persist runtime flips window->settings. Tauri has no fullscreen event, so `onFullscreenChange` derives flips from the `onResized` stream (filtered to actual changes). Persisted as `Settings.windowFullscreen` in `settings.json` (NOT theme.json/keymap.json). **`windowFullscreen` is EXCLUDED from `saveChrome`'s `Omit<Settings, "theme"|"shortcuts"|"windowFullscreen">`** - it's written separately (by the sync's `saveWindowFullscreen`), so a chrome write must merge it from current settings or a sidebar/console toggle would wipe it (same hazard as theme/shortcuts). Requires `core:window:allow-set-fullscreen` in [src-tauri/capabilities/default.json](src-tauri/capabilities/default.json). The `WindowFullscreenSync` bridge mounts inside `SettingsProvider` in `__root.tsx`.

- **Customizable keyboard shortcuts** (ported from `requi`, with a `scope` added): every app shortcut is a `ShortcutAction` in the scoped registry [src/lib/shortcuts/registry.ts](src/lib/shortcuts/registry.ts) (`id`/`name`/`description`/`defaultHotkey`/`scope`, scope = `global`/`tab`/`grid`/`tree`/`editor`). Pure logic: [resolve.ts](src/lib/shortcuts/resolve.ts) (`resolveShortcuts` = override-else-default with `safeNormalize`; `findConflict` is **per-scope** - same combo in two scopes is NOT a conflict), [to-codemirror-key.ts](src/lib/shortcuts/to-codemirror-key.ts) (`Mod+Enter`->`Mod-Enter`), [match-hotkey.ts](src/lib/shortcuts/match-hotkey.ts). Overrides are a sparse `Settings.shortcuts` map persisted in a separate `keymap.json` (mirrors the `theme.json` split in [tauri-store.ts](src/lib/settings/tauri-store.ts)); `saveShortcut`/`resetShortcut` on the settings context. **Do NOT dispatch via `@tanstack/react-hotkeys`' `useHotkeys`** - under jsdom the lib resolves the test platform to "linux" so `Mod`->Control only, but every dbui test fires meta OR ctrl. Instead each component keeps its own window-keydown listener and matches with `matchesHotkey` (where `Mod` = `metaKey||ctrlKey`); the lib is used only for the recorder, normalize/validate, and `formatForDisplay`. Editor keys (`run-query`/`save-script`) bridge into the CodeMirror keymap via `toCodeMirrorKey` in [sql-editor.tsx](src/components/workspace/sql-editor.tsx). The PC forward-`Delete` key stays a FIXED alias to delete-rows/delete-nodes (only `Backspace` is the rebindable default). `persistChrome` in [src/routes/index.tsx](src/routes/index.tsx) MUST preserve `shortcuts` (and `theme`) across a chrome/layout write or a layout change silently wipes the keymap. Components read overrides via `useSettingsOptional` (like `useThemeOptional`) so they still render outside a `SettingsProvider`. The palette derives its hints from the resolved binding (`command-registry.ts` carries an `actionId`, not a hardcoded `hint`). Palette commands carry a `group` (`Create`/`Tabs`/`View`) and render under `CommandGroup` headings in `PALETTE_GROUP_ORDER` order (a group with no visible commands renders nothing) - keep new commands assigned to a group so the palette stays scannable, not a flat mishmash.

## Engines: SQL vs MongoDB (dispatch seam)

- The SQL engines (Postgres/MySQL/SQLite) run on `sqlx::Any` in [src-tauri/src/db.rs](src-tauri/src/db.rs). **MongoDB is NOT a `DbEngine`/`sqlx` arm** - it lives in [src-tauri/src/mongo.rs](src-tauri/src/mongo.rs) (the `mongodb` crate) with its own client registry. `sqlx::Any` cannot drive Mongo; never add a `DbEngine::Mongo` to `db.rs`.
- [src-tauri/src/lib.rs](src-tauri/src/lib.rs) is the dispatcher: every connection-addressed command routes to the Mongo path when `mongo::is_connected(id)`, else the SQL path. `connect_database` takes a raw `serde_json::Value` and peeks the `engine` tag (`mongodb` -> `MongoConfig`, else the SQL `ConnectionConfig` enum). Keep new per-connection commands dispatching the same way.
- The Mongo path returns the SAME IPC structs as SQL (`TableRef`/`TableRows`/`QueryOutcome`/`RowMutation`/`TableStructure`) so the frontend stays engine-agnostic. The shared `RowMutation::Replace` (full-document `replaceOne`) is Mongo-only - the SQL `build_mutation` returns `Err` for it.
- **Schema introspection (F6)**: `connect_database` returns `ConnectCatalog { tables, views }` (NOT a bare `Vec<TableRef>`) - `views_query` (`table_type='VIEW'` PG/MySQL, `type='view'` SQLite) runs alongside `catalog_query` on connect; a views-query failure degrades to empty rather than failing the connect; Mongo returns no views. `fetch_table_structure(id, schema, table)` -> `TableStructure` (columns/indexes/foreign_keys/constraints), dispatched Mongo-vs-SQL in `lib.rs` like every command; SQL builds it from four pure per-engine query builders (`structure_columns_query`/`index_query`/`foreign_key_query`/`constraint_query`, each `(engine, has_schema)`) + the `fold_indexes`/`fold_foreign_keys` pure grouping helpers (composite index/FK = one-row-per-column folded by name); Mongo fills indexes only via `list_indexes`. SQLite check-constraints are NOT exposed by pragma (they live in DDL text) so `constraint_query` returns UNIQUE only there.
- **File logging is emitted at the `lib.rs` dispatcher, not inside `db.rs`/`mongo.rs`** - one call site per command (connect/disconnect/execute_sql/execute_mongo/apply_mutations) covers BOTH engines with no duplicate lines. Each site wraps the awaited call in `std::time::Instant`, then `log::info!`/`error!` a `key=value` line built by a PURE formatter in [src-tauri/src/logging.rs](src-tauri/src/logging.rs) (`format_connect_ok`/`_err`, `format_disconnect`, `format_query_ok`/`_err`, `format_mutations`) - the formatters take primitives + pre-computed millis (clock-free, unit-tested), mirroring the `launch_log_name` pure/impure split. A cancelled connect/query (the `db::CANCEL_SENTINEL` string) logs NO error line - guard with `is_cancel_sentinel` before the `log::error!` (same rule the FE uses to suppress its toast). One line PER command invocation, not per `;`-statement (the History tab already gives per-statement granularity in-app). Logs land in the per-launch `dbui-<ts>.log` (`logging::init`, best-effort) + stdout. To log a new command, add a formatter + a dispatcher call site the same way; don't sprinkle `log::` through the engine modules.
- Build BSON from JSON via the hand-rolled `Value -> Bson` map in `mongo.rs`, NOT `bson::to_document`: the crate-wide serde_json `arbitrary_precision` feature makes `to_document` emit a `$serde_json::private::Number` wrapper Mongo rejects.
- Frontend per-engine differences (History preview strings, filter syntax) live behind `queryPreview(engine)` in [src/components/workspace/query-preview.ts](src/components/workspace/query-preview.ts) + a `mongodb` JSON CodeMirror mode in [src/components/workspace/sql-editor.tsx](src/components/workspace/sql-editor.tsx). Add to the strategy, don't sprinkle `engine === "mongodb"` through the table card.
- The Mongo **Query tab reuses the SQL editor pane** (`SqlTab`/`SqlPane`): same saved-script document tabs, Run/Cancel, History. `SqlTab` picks the executor by `node.engine` (`executeMongo` vs `executeSql`). Mongo commands are self-contained `db.<coll>.<op>(...)` (collection in the text, no picker), `;`-separated; backend `parse_command` + `run_command`/`run_query` in `mongo.rs`. **Both reads AND writes run**: reads `find`/`aggregate` (capped by `limit`, return rows); writes `insertOne`/`insertMany`/`updateOne`/`updateMany`/`deleteOne`/`deleteMany`/`replaceOne` (return an affected count, `returns_rows:false`). `parse_command` splits the args between the outer parens on TOP-LEVEL commas (`split_args`, brace/bracket/string-aware) so two-arg writes (`updateOne(filter, update)`) parse; `MongoCommand.args: Vec<String>` (was a single `arg`). Each write arg is parsed to a BSON document via the SAME `json_object_to_document`/`validate_extended_json`/Extended-JSON path the filter uses (so `{"$oid":...}` works in a filter). Writes are blocked upstream when the DB is read-only (see F11 / `isWriteMongo`), never in `run_command`. Do NOT reintroduce a collection picker or a separate mongo query component.
- Mongo **autocomplete**: in the Query tab `db.` -> collection names, `db.<coll>.` -> the read + write ops (find/aggregate/insertOne/insertMany/updateOne/updateMany/deleteOne/deleteMany/replaceOne), inside a read OR write command body after a `"` -> that collection's sampled field names; in the **filter row** (a bare find document, no `db.<coll>.<op>` prefix) after a key-opening `"` -> the fields of the collection the card is scoped to (`defaultTable`). Fields come from `mongo::fetch_schema` (samples ~50 docs/collection on connect -> `TableSchema` per collection) flowing through the same `databaseSchemas` -> `schema` prop as SQL. Completion lives in `mongoCommandSource` in [src/components/workspace/sql-editor.tsx](src/components/workspace/sql-editor.tsx) - it resolves the collection from the `db.<coll>.<op>` prefix, else falls back to `defaultTable`.
- ObjectId/date `_id` (or any BSON type) in a filter uses **Extended JSON**: `{"_id":{"$oid":"..."}}`, `{"$numberLong":"42"}`, `{"$date":"..."}`. Plain JSON can't match an ObjectId. Decoded in `json_value_to_bson`/`extended_json_type`; a malformed wrapper is a filter error.
- Live Mongo smoke (needs the docker test-stack up): `cargo test --manifest-path src-tauri/Cargo.toml live_mongo -- --ignored`. Test-stack Mongo is host port 27018 (root user `dbui`/`dbui`, `authSource=admin`).

## Learning from conversation

If during a session you learn something project-specific that future-you would otherwise have to re-derive - a non-obvious convention the user prefers, a constraint that bit us, a gotcha worth recording - append it to [docs/learnings.md](docs/learnings.md). Examples: formatting rules the user repeated, gotchas that broke a hook/CI, naming conventions enforced via review.

For architectural trade-offs (significant, costly-to-reverse, or contested choices) use [docs/adr.md](docs/adr.md) instead - that's a separate log.

For UI visual/interaction conventions (corners, borders, density, grids, color, a11y) use [docs/design.md](docs/design.md). A UI rule the user wants enforced goes there, NOT in learnings.md or inline in CLAUDE.md.

Don't add: one-off task context, debugging notes, things obvious from the code itself, or anything that would fit better in [README.md](README.md). Don't ask permission for small additions - just keep the file tight and the diff visible in the next commit.

## Features

- Each feature lives in its own folder: `docs/features/<timestamp>-<slug>/`.
  - `<timestamp>` = `YYYYMMDDHHMMSS` (creation time). `<slug>` = short kebab-case name.
  - Example: `docs/features/20260618193518-bootstrap/`.
- Every feature folder holds two files:
  - `spec.md` - what + why. Follows the spec template structure (overview, acceptance criteria, user test cases, data model, edge cases, dependencies).
  - `plan.md` - how. Follows the plan template structure (task breakdown, execution order, file changes, acceptance verification).
- Adding a new feature:
  1. Create the folder with current timestamp + slug.
  2. Write `spec.md` first. Get it approved before planning.
  3. Write `plan.md` from the approved spec.
  4. Log any significant choices made while specing to [docs/adr.md](docs/adr.md).
- Branch naming: when working on a feature (not a quick fix), the branch name must match the feature's folder name under `docs/features/` exactly (e.g. folder `20260618223203-layout` -> branch `20260618223203-layout`). Quick fixes are exempt.

## Architectural Decisions

- Log only significant, costly-to-reverse or contested decisions to [docs/adr.md](docs/adr.md).
- Significant = changes architecture/data model, hard to undo later, or had real alternatives debated. NOT routine config (script aliases, package manager, default lib options).

## Before committing

- Check whether the change makes README.md or CLAUDE.md drift:
  - New script / removed dependency / renamed module -> update README.
  - New convention or gotcha that future-you would miss -> add to CLAUDE.md (or docs/learnings.md).
  - Removed feature or file referenced in either doc -> remove the reference.
- No duplicates between README.md and CLAUDE.md. Each fact lives in exactly one place:
  - README.md = onboarding facts a human needs to run the app: install steps, commands, repo layout sketch.
  - CLAUDE.md = working rules for an agent editing this repo: conventions, gotchas, "how to add a feature", invariants.
  - If a fact would fit both, put it in CLAUDE.md and link from README only if a human reader needs the pointer.
- If neither doc needs to change, say so explicitly in the pre-commit summary so it's a deliberate decision, not an oversight.

## TDD

Write code red-green-refactor:

1. Red - add a failing test that pins the behaviour you want. Run the relevant suite and confirm it fails for the right reason (not a typo, not a missing import).
2. Green - write the smallest production change that makes it pass. No speculative branches, no helper extraction yet.
3. Refactor - once green, clean up names, extract duplication, tighten types. Tests stay green throughout.

Two test layers, pick the one that owns the behaviour:
- Frontend (React/TS) -> `npm test` (Vitest).
- Rust backend / Tauri commands -> `cargo test` in `src-tauri/`.

Don't skip red. A test that's never seen failing is a test you can't trust. Don't refactor on red - get to green first, then improve.
