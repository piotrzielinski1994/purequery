# Learnings

Project-specific conventions, gotchas, and constraints worth recording so future-you (human or agent) doesn't re-derive them. Append-only. For architectural trade-offs use [adr.md](adr.md) instead.

## Entries

<!-- Format: one bullet per learning. Date prefix optional. -->

- TanStack Hotkeys split: `@tanstack/hotkeys` is the framework-agnostic core (no React hook). The React `useHotkey` + `HotkeysProvider` live in `@tanstack/react-hotkeys`. Install the adapter, not the core.
- Hotkey strings are case-sensitive in the typed union: use uppercase keys, e.g. `"Mod+K"` not `"Mod+k"`.
- Under jsdom the hotkeys lib resolves `Mod` to `Control` (test platform reports non-mac), so hotkey tests fire `{Control>}k{/Control}`, not Meta.
- ESLint react-hooks v7 false-positives: `useReactTable` trips `react-hooks/incompatible-library`; code-based TanStack route files trip `react-refresh/only-export-components`. Both scoped off in eslint.config.js for the relevant paths.
- shadcn Button keeps `react-refresh/only-export-components` as an accepted warning (canonical upstream file exports `buttonVariants` alongside the component). Lint exits 0 with warnings.
- Bootstrap scaffold was ported from the sibling `requi` repo (same Tauri 2 + React 19 + TanStack stack), not generated fresh from `npm create tauri-app`. Identity rewritten: package name, Cargo `name`/`_lib`, `tauri.conf.json` productName/identifier, `main.rs` lib ref.
- shadcn `resizable` ships for the old `react-resizable-panels` API: it passes `direction` to `ResizablePanelGroup`, but v4 renamed the prop to `orientation` ("horizontal"|"vertical"). Fix the prop at call sites or typecheck fails.
- react-resizable-panels v4 `Panel` size props (`defaultSize`/`minSize`/`maxSize`) read a bare `number` as PIXELS, not percent. `defaultSize={20}` = 20px wide. For proportional panels pass a string with a unit: `defaultSize="20%"`.
- jsdom has no `ResizeObserver`; radix Select/Tabs + react-resizable-panels need it. A no-op `ResizeObserver` stub is installed in `src/test/setup.ts`. Also expect harmless "Not implemented: Window's scrollTo()" noise from shadcn `ScrollArea` under jsdom.
- radix `SelectValue` renders nothing until the dropdown opens (items live in an unmounted Portal), and jsdom can't open it. To assert the current value in tests, render the value as explicit `SelectTrigger` children instead of relying on `<SelectValue/>`.
- A tree query leaf's `aria-selected` tracks `activeQueryId` (the active tab), not `selectedNodeId` - clicking a folder must not visually deselect the active query (spec E-2). Folder rows track `selectedNodeId`.
- `getByText` regexes for a panel's empty-state must not also match a nearby `Select` trigger label: the "none" connection body said "No connection auth" while the type select also offered "No Auth", so the test matched two nodes. Renamed the select option to "None" to disambiguate.
- A `react-resizable-panels` group mounted anywhere in a `radix Tabs` subtree breaks tab-switching under jsdom: `user.click`/`fireEvent.click` on a `TabsTrigger` becomes a no-op (state never commits), regardless of `forceMount` or whether the group is inside `TabsContent`. Its global pointer/resize handlers interfere with the click. Consequence: keep resizable splits at the shell level (sidebar|content, content|console) and use a plain fixed flex split inside a tab panel (e.g. the SQL tab's editor|results). Don't nest a ResizablePanelGroup under tabbed content.
- Dual-action tree row (chevron toggles children, row body opens a tab): make the chevron a real `<button aria-label="Toggle <name> tables">` with `onClick` calling `event.stopPropagation()` before toggling, nested inside the clickable `role="treeitem"` div. Without stopPropagation the row's open-handler also fires. Tests target the two actions separately: `within(row).getByRole("button", {name:/toggle .* tables/i})` for expand, clicking the treeitem for open.
- This is a DB client ported from the HTTP-client `requi`: watch for HTTP-isms that don't fit. The auth model came over as a `none/bearer/basic` union -> mapped to `none/password/token` and surfaced as an "Auth type" select. Wrong for databases: a DB connection is host/port/database/user/password (fixed fields), not a tagged auth union. Dropped the `Connection` union entirely; `DatabaseNode` now carries those fields flat. Lesson: when reusing requi's structure, re-judge each domain concept, don't 1:1 map HTTP semantics.
- Dev server port is 1431 (not the create-tauri-app default 1420), pinned in BOTH `vite.config.ts` (`server.port`) and `src-tauri/tauri.conf.json` (`build.devUrl`) - they must match or `tauri dev` hangs "Waiting for your frontend dev server". Tauri does not interpolate env vars in `devUrl`, and `tauri dev --port` controls a different (built-in) server, so a single env override can't drive both without a wrapper (see tauri issue #6186); a hardcoded matching pair is the simplest fix.
- `cmdk` calls `Element.scrollIntoView` when navigating its list; jsdom has no such method, so a `command-palette` test using cmdk throws without a stub. Added a no-op `Element.prototype.scrollIntoView` in `src/test/setup.ts` (next to the `ResizeObserver` stub).
- Command palette is `cmdk` + a radix `Dialog`, reusing the **`radix-ui` umbrella** (`import { Dialog as DialogPrimitive } from "radix-ui"`) like `ui/tabs.tsx`, NOT the standalone `@radix-ui/react-dialog` that `requi` uses. Only `cmdk` was added as a new dep. The radix `Dialog` needs a `DialogTitle` + `DialogDescription` rendered INSIDE `DialogContent` (sr-only) for the accessible name, else radix logs missing-title warnings; `ui/command.tsx`'s `CommandDialog` does this.
- cmdk filters by hiding: a non-matching `CommandItem` returns `null` (unmounts), so tests can assert `queryByText(name).not.toBeInTheDocument()` after typing. `CommandEmpty` renders only when the filtered count is 0.
- `sonner`'s `<Toaster theme="system">` calls `window.matchMedia` on mount; jsdom doesn't define it, so any test rendering `WorkspaceLayout` (which now mounts `<Toaster/>`) throws "window.matchMedia is not a function". Stubbed unconditionally in `src/test/setup.ts` (an `"in" guard does NOT work - jsdom has the property as a throwing getter, not absent). Mock `sonner` with `vi.mock("sonner", () => ({ toast: { success: vi.fn(), error: vi.fn() } }))` to assert toast calls.
- A status indicator with an `aria-label` rendered INSIDE a `role="treeitem"` leaks into the row's computed accessible name (name-from-content), so `getByRole("treeitem",{name:"admin_db"})` breaks once a "admin_db connected" dot appears. Pin the row name with an explicit `aria-label={node.name}` on the treeitem (TableRow already did this); the dot stays independently queryable via `getByLabelText(/admin_db connected/)`.
- Radix `Select` can't be opened under jsdom (Portal + no layout), so a test can't click an option to assert engine-switch wiring end-to-end. Cover the value-follows-state leg by seeding/asserting the trigger text; trust `onValueChange` by inspection (or move to an e2e/browser test). The connect form seeds the engine from `DatabaseNode.engine` and renders the label as explicit trigger children (not `<SelectValue/>`).
- Tauri command args: `invoke("connect_database", { config })` - the object key (`config`) must match the Rust command's parameter name exactly, else deserialization fails at runtime (typecheck won't catch it). Serde enum `#[serde(rename_all="lowercase")]` maps Rust `Postgres`/`Mysql` <-> TS `"postgres"`/`"mysql"`.
- `sqlx::Any` driver can't decode every native column type. Postgres `information_schema.table_name` is the `name` type -> `fetch_all` via `AnyPool` fails at runtime (not at connect) with "error in Any driver mapping: Any driver does not support the Postgres type PgTypeInfo(Name)". Fix: cast to a portable type in the query (`SELECT table_name::text ...`). The connection itself succeeds; the error surfaces only when decoding the result row. Caught in manual testing against a real PG (DBeaver connected fine; our query errored). Pure unit test pins the `::text` cast since no live DB in CI.
- The `Any`-decode trap generalizes to ALL table content: arbitrary user columns (uuid, timestamptz, numeric, jsonb, ...) are equally undecodable. Strategy for browsing rows: read column names from `information_schema.columns` (parameterized, ordinal order), then `SELECT <each col>::text` (PG) / `CAST(<col> AS CHAR)` (MySQL) so every value comes back as a portable string. Decode as `Option<String>` to preserve NULL. Quote identifiers per engine (`"x"` PG / `` `x` `` MySQL, doubling the quote char) to stay injection-safe since table/column names can't be bind params. Pure query-builder fns (`rows_query`/`columns_query`/`quote_identifier`) are unit-tested; the DB round-trip isn't (no live DB in CI).
- Adding `useQuery` to a workspace component means its tests need a `QueryClientProvider` wrapper (with `retry:false` so rejected fetches surface immediately instead of retrying). Mock `@/lib/tauri`'s fetch fn; `mockReturnValueOnce(new Promise(() => {}))` (never-resolving) asserts the loading state.
- `useReactTable` requires STABLE `data` and `columns` references. Building them inline (`rows.map(...)`, `columns.map(columnHelper.accessor...)`) returns fresh arrays each render; combined with any state-driven re-render of the grid this triggers TanStack Table's infinite re-render loop, which pegs the main thread and FREEZES the whole app (clicks/keyboard dead) - it manifests as "nothing is clickable", not a console error. It stayed dormant while the grid had no state; adding row-selection (`useState`) was the first thing to re-render it and exposed it. Fix: wrap both `data` and the column defs in `useMemo`, and memoize the arrays passed in by parent wrappers too (a parent that `.map()`s fresh arrays every render defeats the child's memo). The tiny test fixtures (2-3 rows) did NOT reproduce the loop, so unit tests passed while the real app froze - a reminder that jsdom won't catch a perf/loop bug.
- The table card and SQL result pane share ONE grid (`data-grid.tsx` `DataGrid`) - they must always look identical (user-enforced invariant, see CLAUDE.md). It's edit-capable; read-only callers pass `editable={false}` + no-op handlers. Headers always render (even with zero rows) so an empty result still shows its column structure with "No rows." beneath. Don't fork a second grid.
- A resizable split INSIDE a radix Tabs panel can't use `react-resizable-panels` (its global pointer/resize handlers break tab-switching under jsdom - see the older entry). The SQL editor|results split is a hand-rolled `HorizontalSplit` (`horizontal-split.tsx`): percentage width clamped 15-85%, `window` pointermove/pointerup drag, `role="separator"`. jsdom returns `getBoundingClientRect().width === 0`, so the drag math no-ops in tests unless you mock the rect - the split tests stub `getBoundingClientRect` to assert clamp/drag/release.
- The `useReactTable` stable-ref freeze recurs in EVERY grid, not just the table browser. It bit `OutcomeGrid` (SQL-tab results) too: after Run, `addHistoryEntry` mutates the workspace context value -> SqlEditor re-renders -> `OutcomeGrid` rebuilt `data`/`columns` as fresh arrays -> infinite re-render -> whole app frozen (query had already succeeded, 38 rows visible, then dead). Any NEW grid must `useMemo` its `data` AND column defs from the moment it's written - don't wait for a state-driven re-render to expose it. Rule of thumb: if a component calls `useReactTable`, its `data`/`columns` args must be memoized, period. with `role="separator"`+`aria-label` leaks into the columnheader's accessible name (same name-from-content trap as the sidebar status dot), breaking `getByRole("columnheader",{name:"id"})`. Make the handle `aria-hidden` (it's a mouse affordance, not a named control) and target it in tests via `data-testid`.
- Server-side row filter must bind the pattern as a param, never interpolate: `<col>::text ILIKE $1` (PG, case-insensitive) / `CAST(<col> AS CHAR) LIKE ?` (MySQL). PG reuses `$1` for every OR'd column (one bind); MySQL needs one `?`+bind per column. Validate the filter column against the known column list and fall back to all-columns if unknown - the column name can't be a bind param. Builder returns `(sql, Vec<bind>)` so it stays pure/unit-testable.
- Running arbitrary user SQL through `sqlx::Any`: you don't know the result columns up front and Any can't decode native types, so (1) `pool.prepare(sql)` then `.columns()` to discover column names WITHOUT executing - an empty column list means a non-row statement (UPDATE/INSERT/DDL) -> run via `pool.execute()` and report `rows_affected()`; a non-empty list means row-returning -> (2) wrap as `SELECT <col>::text FROM (<user sql>) AS dbui_q LIMIT N` so every column decodes as text. Strip a trailing `;` before wrapping or the subquery is a syntax error. Needs `use sqlx::{Column, Executor, Statement}` for `.columns()`/`.name()`/`.prepare()`/`.execute()`.
- `row_to_json` recovers column names ONLY from rows that exist - a zero-row query emits zero keys, so the SQL result grid had no headers while the table card (which reads columns from `information_schema`) always did. Same `DataGrid`, different data. Fix: when the wrapped query returns 0 rows, run a column probe. FIRST attempt `... LEFT JOIN LATERAL (<sql>) dbui_q ON true` + `row_to_json(dbui_q)` FAILED: `row_to_json` over the join-nullable side of an unmatched LEFT JOIN is a whole-row NULL composite (emits no keys at all), not an object of null-valued keys. The working probe re-selects the lateral columns into a derived table so the composite is non-null: `SELECT row_to_json(dbui_cols)::text FROM (SELECT dbui_q.* FROM (SELECT 1) dbui_base LEFT JOIN LATERAL (<sql>) dbui_q ON true LIMIT 1) dbui_cols`. That derived row has individually-null but defined columns, so `row_to_json` emits every key (`wrap_columns_probe` + the `columns.is_empty()` branch in `run_query_postgres`). Lesson: `row_to_json(x)` where `x` is a nullable composite gives JSON `null`, not `{keys:null}` - wrap it in a derived table to force a real row.
- The above `prepare().columns()` trick is BROKEN on Postgres: `prepare()` makes Any *describe* the result, which maps every result column's type to `AnyTypeInfo` - and native types (timestamp, uuid, numeric, ...) have no Any mapping, so `prepare()` itself errors ("Any driver does not support the Postgres type PgTypeInfo(Timestamp)") BEFORE the text-cast wrap ever runs. `SELECT * FROM product` failed for exactly this reason while `fetch_table` worked (the latter never prepares - it reads column names from `information_schema` and casts to text). Fix: don't prepare arbitrary SQL on Postgres at all. Classify by leading keyword (`is_row_returning`: SELECT/WITH/VALUES/TABLE/SHOW/EXPLAIN, skipping leading `--`/`/* */` comments); execute non-row statements for `rows_affected()`; wrap row-returning ones as `SELECT row_to_json(dbui_q)::text FROM (<sql>) AS dbui_q LIMIT N` - one text column Any always decodes - then parse the JSON in Rust. Use `row_to_json` (not `to_jsonb`) to preserve column order, plus serde_json's `preserve_order` feature so the parsed `Map` keeps it. MySQL keeps the `prepare().columns()` path (its Any type mapping is fine for the describe). Adding `arbitrary_precision` to serde_json keeps large numerics/ints exact when re-stringified.
- Writing back via `sqlx::Any`: Postgres errors on a bound text value assigned to a non-text column (no implicit coercion), so an `UPDATE` must cast to the column's real type: `SET "col" = $1::<udt_name>` (read `udt_name` from `information_schema.columns`). MySQL coerces strings, so it binds plainly. Match the pk as text (`"pk"::text = $2`) to avoid per-type pk handling. This is the write-side twin of the read-side "cast everything to text" trap.
- `EXPLAIN` / `EXPLAIN ANALYZE` / `SHOW` are row-returning but CANNOT be subquery-wrapped: the Postgres path wraps row-returning SQL as `SELECT row_to_json(dbui_q) FROM (<sql>) AS dbui_q`, and PG rejects EXPLAIN/SHOW inside a subquery -> "syntax error at or near \"select\"" (the error points at the wrapper's SELECT, not the user SQL - misleading). Fix: split classification - `is_row_returning` (run vs execute) AND `is_subquery_wrappable` (SELECT/WITH/VALUES/TABLE only). EXPLAIN/SHOW are row-returning-but-not-wrappable -> fetch DIRECTLY via `fetch_plain_text_rows` (their output columns - `QUERY PLAN`, setting - are already text, so Any decodes them; column names come from the first row's `.columns()` metadata). MySQL is unaffected (its `prepare().columns()` describe path handles EXPLAIN fine).
- `information_schema.columns.udt_name` (PG) gives the castable type name (`int4`, `uuid`, `timestamptz`); `data_type` is the verbose SQL-standard form. Use `udt_name` for `::cast`.
- "Filter" in a DB grid means a raw SQL WHERE to power users (they type `price > 10`), NOT substring search. We shipped substring first and it matched nothing - confirm filter semantics before building. Raw SQL can't be parameterized, so it's appended verbatim (the user owns SQL-injection in their own query against their own DB; a bad expression just returns a DB error toast).
- Any test that renders `WorkspaceLayout`/`Content`/`TableCard` now needs a `QueryClientProvider` ancestor (TableCard calls `useQuery` unconditionally, even when `enabled:false`). Shared `src/test/query-wrapper.tsx` exports `QueryWrapper`; pass it via RTL's `{ wrapper }` option (wrap the local `render` once rather than editing every call site).
- Don't hijack the global `Tab` key unconditionally. The table-card's "Tab toggles grid<->record view" listener is `window`-level + `preventDefault`; guard it to skip when `event.target` is an `<input>`/`<textarea>` so the filter field stays normally tabbable.
- The Cmd/Ctrl+K palette listener lives on `window` in `WorkspaceLayout` (`useEffect` + cleanup). It opens unconditionally (`setIsPaletteOpen(true)`, never toggles) so re-pressing while open is a no-op, and `preventDefault()` always fires for the combo to suppress the browser default. Palette commands are gated by a `when(state)` predicate per definition in `command-registry.ts` (strategy gating) - keeps the JSX free of tab-count ifology.
- A database's table leaves render ONLY when its `connectionStatus === "connected"` (`DatabaseRow` in `tree-row.tsx`, gated `isExpanded && isConnected`). The mock tree ships hardcoded tables, but the app can't know a DB's real tables until a `connect_database` fetch returns the live catalog - showing mock tables for an idle/error DB was a bug (visible in a screenshot before any connect). Tables always come from `setDatabaseTables` after a real connect. Tests touching table leaves must seed `initialConnectionStatus={[[id,"connected"]]}` (the prop exists for this) or do a real (mocked) connect first.
- Persisted connections restore the CONFIG only, NOT a `"connected"` status. Seeding restored connections as `"connected"` was a trap: `useAutoConnect` only fires on `status === undefined`, so a pre-seeded status skipped the live catalog fetch and the stale mock tables showed. Restore config -> auto-connect on open -> real fetch. No re-type, no stale tables. `initialConnectionStatus` is a separate WorkspaceProvider prop from `initialConnections` precisely so persistence seeds config without faking liveness.
- Corollary to the "resizable group breaks tab-switching" entry: a test that must reach a database **sub-tab** (SQL/Views/Script/Settings) CANNOT click it through `WorkspaceLayout` - both the sidebar split AND the console split swallow the click (confirmed on `main`, pre-existing, not a regression). To test sub-tab behavior, render the sub-tab component directly alongside the siblings you need to observe (`<ContentHeader/><SettingsTab/><SidebarTree/>` under one real `WorkspaceProvider`), the `settings-tab.test.tsx` pattern - same production wiring via `useWorkspace`, no panel group. Programmatic sub-tab switches (e.g. `addDatabase` setting Settings) work fine through the full layout; only pointer clicks on the trigger are swallowed.
- Adding a database/folder from the empty state: tree mutation is owned by `workspace-context.tsx` (`setTree` is private there) - exposed as intent-named actions `addDatabase`/`addFolder`/`renameDatabase`, never a raw setter. `addDatabase` seeds `connectionStatus` `"idle"` so `useAutoConnect` skips it (a fresh DB has blank/invalid defaults; auto-connecting would error-toast immediately). New nodes append at root; a fresh `crypto.randomUUID()` id each. The Settings "Name" field writes the tree directly via `renameDatabase` (NOT the local connection `form` state) so the sidebar row + tab title update live.
- `useAutoConnect` fired `connect()` TWICE on opening a database (two "Connected - N tables" toasts, two catalog fetches). Cause: `<React.StrictMode>` double-invokes effects in dev, and `setConnectionStatus("connecting")` is async, so both invocations read the same stale `status === undefined` and the `connecting` guard inside `connect()` also reads stale state. State-based guards can't dedup within a single commit. Fix: a `useRef(new Set<string>())` of already-attempted ids in `useAutoConnect`, checked + added synchronously before calling `connect`. Only reproduces under StrictMode, so a non-StrictMode test misses it - the regression test wraps the render in `<StrictMode>`.
- Revisiting a table tab (open product -> switch to the DB tab -> back to product) refetched the rows and appended a duplicate History row each time. Cause: only the active tab's card is mounted, so leaving + returning UNMOUNTS/REMOUNTS `LiveTable`; with the default `staleTime: 0` the query refetches on remount, and the `[dataUpdatedAt]` mount-effect re-logs history. Two fixes together: (1) `staleTime: Infinity` on the `["table-rows", ...]` query - a new filter uses a different queryKey and Save calls `invalidateQueries`, so caching only suppresses the pointless revisit refetch; (2) `addHistoryEntry` dedups by `id` (the fetch id is deterministic: `fetch-<tableId>-<filter>-<dataUpdatedAt>`), which also kills the React key collision from the duplicate. jsdom unit tests reproduce remount via a `visible` toggle rerender that swaps the card for a placeholder.
- SQLite engine (`DbEngine::Sqlite`): catalog reads `sqlite_master` (`WHERE type='table' AND name NOT LIKE 'sqlite_%'`); columns/pk/types come from `pragma_table_info(?)` (parameterized, ordered by `cid`; pk via `WHERE pk > 0 ORDER BY pk`). Quote idents with double-quotes (same as Postgres). Cast every value to text with `CAST(<col> AS TEXT)`. UPDATE binds plainly with `?` and matches the pk as text - same as the MySQL arm. `build_url` -> `sqlite://<path>`; the path is percent-encoded with a SQLite-only set that keeps `/` (so separators survive) but encodes spaces - a raw space breaks the connection URL.
- SQLite shares MySQL's `run_query` path: both let `sqlx::Any` describe a prepared statement (`prepare().columns()`) without the Postgres exotic-type describe failure, so `run_query_mysql` was renamed `run_query_prepared(engine, ...)` and SQLite routes through it. If a live SQLite ever fails the describe like Postgres does, fall back to the json-wrap path (untested - no live DB in CI).
- `ConnectionConfig` is an engine-tagged discriminated union, NOT a flat struct: a network variant (postgres/mysql with host/port/database/user/password) and a sqlite variant (`{ engine:"sqlite", file }`). Rust mirror is a `#[serde(tag="engine", rename_all="lowercase")]` enum; `config.engine()` recovers the `DbEngine` for the per-engine query builders (which still take `DbEngine`). `DatabaseNode = DatabaseNodeBase & ConnectionConfig` so it's a union too - NEVER read `node.host` without narrowing on `node.engine` first (TS errors on the sqlite arm). Use the `connectionOf(node)` helper to extract a `ConnectionConfig` instead of hand-spreading fields; it lives in `src/lib/workspace/model.ts` next to the types. When switching a node's engine, `applyDatabaseConfig` rebuilds from `DatabaseNodeBase` fields + `...config` so stale network fields don't leak onto a sqlite node.
- The Settings form holds a SUPERSET of both connection shapes (`ConnectionForm` with all of host/port/database/user/password/file) so toggling the engine never loses what the user typed; `configFromForm` projects the live `ConnectionConfig` per engine at connect time. Radix Select still can't open under jsdom, so the engine-switch-keeps-values case (TC-011) is only covered indirectly (render a sqlite node vs a postgres node), not by an in-form switch.
- TanStack Query's `refetchOnWindowFocus` defaults to `true`. With our `LiveTable` history effect (which logs a SELECT to History on every `dataUpdatedAt` change), each macOS app-switch back onto the dbui window refetched the open table and logged a phantom SELECT to History. The table snapshot is refetched deliberately (filter / reopen / Save-invalidate), so auto-refetch-on-focus is pure noise here. Set `refetchOnWindowFocus: false` in the root QueryClient defaults (`app/providers.tsx`). The in-app test QueryClient never had focus events, so unit tests didn't surface it - another focus/visibility bug jsdom can't catch.
- Domain types live in `src/lib/workspace/model.ts` (NOT in a `mock-data.ts` - that file is gone). `connectionOf` lives there too (it's logic over `DatabaseNode`, not mock data). Mirrors `requi`'s `src/lib/workspace/model.ts`. `WorkspaceProvider` defaults `tree`/`consoleLines` to `[]`; production always feeds the real tree from the store (`routes/index.tsx`), so there is no mock fallback. The Console tab is empty until real log wiring lands (F9) - that emptiness is intended, not a regression.
- Grid browsing (F2): the table card pages via `useInfiniteQuery` (queryKey `["table-rows", id, filter, sortKey]`) - changing filter/sort swaps the key so paging resets to offset 0 for free; `getNextPageParam` returns `undefined` on a short page (< ROW_LIMIT) to hide "Load more"; `placeholderData: keepPreviousData` keeps the headers (and the click target) mounted while a re-sort/filter loads instead of flashing "Loading...". Sort is server-side `ORDER BY <real col>` (NOT the `::text`-cast alias, so numerics sort numerically); the column is validated against the known list in `build_rows_query` (unknown -> no ORDER BY) since it can't be a bind param. The SQL result grid sorts CLIENT-side (`sortRows`, locale-compare numeric, NULLs last) because arbitrary user-query rows have unknown types and re-running would mean rewriting user SQL. Both share `DataGrid`; the sort arrow + type subheader are `aria-hidden` so they don't leak into the columnheader's accessible name (same name-from-content trap as the resize handle/status dot - `getByRole("columnheader",{name:"id"})` would break otherwise).
- `TableRows.columns` is `TableColumn[]` (`{name,dataType,nullable,isPrimaryKey}`), not `string[]` - changed in F2 #13 so the header can show type + PK/NN, and so F4 autocomplete / F6 schema browser can reuse it. Backend `read_table_rows` zips `columns_query` (names, ordinal order) with `column_types_query` + `nullable_query` via `assemble_columns`; missing metadata degrades (empty type, nullable=true) rather than panicking. Nullable: PG/MySQL `is_nullable` text 'YES'/'NO'; SQLite `pragma_table_info.notnull` 0/1 INVERTED (`read_nullable` collapses both to a bool). Frontend `fetchTable(config, table, {limit, offset, filter, sort})` - opts object, not positional, since 4 optional args.
- F2 follow-up (live-app review): (1) sticky grid header - `th` is `sticky top-0`; with `border-collapse` a real border scrolls away, so the 1px divider is `shadow-[inset_0_-1px_0_var(--border)]` (travels with the cell, still 1px per design.md). The console/history tab bar is already pinned because the `role="tablist"` sits OUTSIDE the `ScrollArea` panes. (2) Sort triangle is ALWAYS rendered on every sortable header (dim `▾` unsorted, solid `▲`/`▼` active), right-aligned via a `justify-between` flex - not just on the active column. (3) Status bar shows `<loaded> of <total>` where total is an unbounded `count_table` (`SELECT COUNT(*)`, honors filter, NO limit) keyed on filter ONLY (sort/paging must not recount) + a page-size **number input** (any positive int; commits on Enter/blur, reverts invalid) that re-fetches from offset 0 (pageSize is in the infinite-query key). Native `<input type=number>`/`<select>` over radix (radix can't open under jsdom); kill the number spinner + own bg/padding (`[appearance:textfield]` + webkit spin-button none, `bg-transparent h-full`) so it sits flush in the status bar. (4) A raw SQL `DROP ...` typed in the filter can't execute: it's wrapped `WHERE (<expr>)` and run as ONE sqlx prepared statement, so a `;`-breakout is a syntax error, not a second command; we ALSO reject a filter containing `;` up front with a toast (defense-in-depth + clearer error). CSV export already quotes any field with `,`/`"`/newline (`csvField`).
- Toggle lag on a large open table (Cmd+B sidebar / Cmd+J console) had TWO causes, both fixed: (a) REMOUNT - the layout swapped the root element type (`ResizablePanelGroup` when a panel was visible vs a bare `<div>` when hidden), so React tore down + rebuilt the entire `<Main/>`/`<Content/>` subtree incl. the 200-row grid's TanStack model on every toggle. Fix: render the group UNCONDITIONALLY and add/remove only the sidebar/console panel as a keyed sibling (`key="content"` stays mounted; react-resizable-panels v4 has no `order` prop, identifies panels by `id`+DOM order). (b) RE-RENDER - the 200-row × N-col grid is the heaviest render in the app and is a workspace-context consumer, so any context-value rebuild (toggling `isSidebarVisible`/`isConsoleVisible`, which are in the value's `useMemo` deps) re-rendered every cell. Fix: `React.memo(DataGrid)` + make ALL its props referentially stable - memoize `rows`/`columnNames`/`columnMeta`/`edits`, `useCallback` the handlers (`commitEdit`/`cycleSort`/`editValueAt`/`isDirtyAt`), AND `useCallback([])` the context actions they close over (`upsertPendingEdit`/`discardPendingEdit`/`discardPendingEditsForTable`/`addHistoryEntry` - functional setters, no deps). memo is useless if a single prop identity churns, so the whole chain up to the context actions must be stable. jsdom can't measure render perf, so unit tests prove correctness not the speedup - verify in the live app.
- DataGrid `<thead>` is `sticky top-0 z-10` so vertical scroll keeps headers visible. Gotcha: the table is `border-collapse`, and a sticky cell's own `border-b` scrolls away with it - so the 1px bottom divider is an INSET box-shadow (`shadow-[inset_0_-1px_0_var(--border)]`) that travels with the sticky cell and stays exactly 1px (design.md: dividers never thicken). The header `<tr>`'s `border-b` was removed to avoid doubling. Sticky `<th>` also needs an opaque `bg-background` (else scrolled rows bleed through) and `sticky` already establishes the positioning context for the absolute resize handle, so don't also add `relative` (CSS conflict warning). The console/changes/history panel header (the tablist) is NOT sticky-styled - it's a flex sibling ABOVE the ScrollArea. But "sibling above a flex-1 ScrollArea" is NOT enough on its own: the flex column needs a height-bounding chain or the whole `<section>` overflows its panel and the tablist scrolls away with it. Fix: `<section>` is `min-h-0 overflow-hidden`, the tablist is `shrink-0`, and each `ScrollArea` is `min-h-0 flex-1` - then the list scrolls INSIDE and the tablist stays pinned.
- Export is clipboard-only (no Tauri dialog/fs plugin): `src/lib/export.ts` `toCsv`/`toJson` are pure (CSV quotes fields with `,"`/newline, doubles embedded quotes, NULL -> empty field / JSON `null`); the shared `CopyButtons` (in `data-grid.tsx`, used by both grids) calls `navigator.clipboard.writeText` + toasts. jsdom has no clipboard - a `navigator.clipboard.writeText` stub lives in `src/test/setup.ts`.
- "Filter" wraps the raw input verbatim in `WHERE (<expr>)` (parenthesized so multi-condition input stays one valid clause); still raw SQL the user owns, NOT a structured builder (#26 was declined - user wanted the simplest "wrap whatever is typed" behavior, malformed -> DB error toast). The History preview SQL mirrors the parens.
- Expected Tauri-boundary failures (connection refused, bad SQL, failed UPDATE) use a `Result<T>` ADT instead of `try/catch` (CLAUDE.md preference). `src/lib/result.ts` exports `Result<T> = {ok:true;value:T}|{ok:false;error:string}` + `toResult(promise)` which wraps a throwing `Promise<T>` (stringifies non-`Error` rejections). Apply `toResult` at the CALL SITE (`use-connection.ts` connect, `table-card.tsx` save) and branch on `result.ok`; the `@/lib/tauri` boundary fns stay throwing so the existing `mockResolvedValue`/`mockRejectedValue` test mocks need no migration. `requi` puts the ADT in the boundary client itself - dbui keeps it at the call site because its tests mock the boundary directly.
- SQL tab editor is CodeMirror 6 (`@uiw/react-codemirror` + `@codemirror/lang-sql`), isolated in `sql-editor.tsx` (widget) + `sql-editor-theme.ts` (Darcula `HighlightStyle`, mirrors requi's JSON editor). jsdom CANNOT simulate CodeMirror keystroke typing - tests reach the live `EditorView` via `EditorView.findFromDOM(container.querySelector(".cm-editor"))` and drive it with `view.dispatch({changes})` / `{selection}`, then assert `view.state.doc.toString()` (the doc spans `.cm-line` divs, so `getByText`/`toHaveValue` won't see multi-line or tokenized SQL). Accessible name "SQL editor" is set on `.cm-content` via `EditorView.contentAttributes.of({"aria-label":...})` (that element is the `role=textbox`). Autocomplete is lang-sql's native `schema` option: `sql({dialect, schema})` where schema is `Record<tableName, columnName[]>` - completes keywords always, tables after FROM, columns after `table.`. A 2nd `syntaxHighlighting(classHighlighter)` is added alongside the Darcula style purely so tests can assert a `tok-*` class is rendered (Darcula uses inline styles, not queryable classes).
- Run-selection (#12): one Run control + `Mod-Enter` keymap both call `submit()`, which reads `selectedOrAllSql(editorRef.current)` (exported from `sql-editor.tsx`) - runs the selection if it trims non-empty, else the whole buffer. The tab keeps a live `EditorView` ref via `onCreateEditor`.
- Autocomplete needs the FULL schema (tables+columns), but `connect_database`/`list_tables` returns table NAMES only (columns are fetched lazily per opened table). Added a separate Rust `fetch_schema`/`schema_query` (one round-trip, all base tables + columns, per-engine; SQLite via `sqlite_master` JOIN `pragma_table_info`) + a pure `group_schema` folder (flat ordered triples -> `Vec<TableSchema>`, relies on `ORDER BY table_name` for contiguity). FE stores it in workspace-context `databaseSchemas: Map<dbId, TableSchema[]>`, fetched on connect (failure -> `[]`, connection stays up), cleared in `removeConnection`. Kept SEPARATE from the `tables` name-list so the connect flow + its tests are undisturbed.
- GOTCHA: adding `fetchSchema` to the connect path broke every test that `vi.mock("@/lib/tauri")` with a `connectDatabase` mock - `useConnectionActions.connect` now also calls `fetchSchema`, and an absent export throws "No X export is defined on the mock", while a bare `vi.fn()` returns `undefined` and `toResult(undefined)` crashes on `.then`. Any tauri mock whose test triggers connect needs `fetchSchema: vi.fn(() => Promise.resolve([]))`.
- The table-card filter row reuses the SAME `SqlEditor` as the SQL tab (single-line mode: `singleLine` prop -> Enter submits instead of Mod-Enter, `basicSetup` drops line numbers/fold/active-line, an `EditorState.transactionFilter` blocks newline-introducing transactions, `height="auto"`). Same Darcula highlight + table/column autocomplete, one component. Don't fork a second editor - parallels the ONE-grid rule for the data grid. Filter tests can't `user.type` into it either (jsdom + CodeMirror): a `typeFilter` helper in table-content.test.tsx dispatches on the live filter `EditorView` (found via `.closest(".cm-editor")` + `EditorView.findFromDOM`), then submit via the Run-filter button (or focus `.cm-content` + `{Enter}`).
- F5 reversed "Connect is stateless": the backend now HOLDS a pool per database id in a process-wide `static POOLS: LazyLock<Mutex<HashMap<id, HeldConnection>>>` (`HeldConnection { pool, engine }`), opened by `connect_database(connectionId, config)`, closed by `disconnect_database(connectionId)`. Every other command (`fetch_table`/`count_table`/`apply_mutations`/`execute_sql`/`fetch_schema`) takes `connection_id` first and resolves the pool via `with_pool(id) -> Result<HeldConnection, String>` ("not connected" Err for an unknown id, no panic). `config` is sent ONLY on connect. `with_pool` is SYNC (clones the `Arc`-backed `AnyPool` + `Copy` `DbEngine` out of the lock) so the mutex is never held across an `.await`. Consequence for FE tests: any `vi.mock("@/lib/tauri")` whose test hits disconnect needs `disconnectDatabase: vi.fn()`, and all command assertions are id-first (`toHaveBeenCalledWith("db-ppp", ...)`, NOT the `config` object). `executeSql` now resolves a `QueryOutcome[]` ARRAY and takes a 3rd `requestId` arg.
- Multi-statement: `split_sql_statements(sql)` (pure, in db.rs) is a char-scanner that splits only on a TOP-LEVEL `;` - a `;` inside `'...'`, `"..."`, `` `...` ``, `-- line comment`, `/* block */`, or a Postgres `$$`/`$tag$` dollar-quote does NOT split. `dollar_tag_length` returns None for `$1` placeholders (char after `$` is a digit then a non-`$`), so bind placeholders are safe. Blank/comment-only statements are dropped (reuses `strip_leading_noise`). The batch runs each statement IN ORDER on ONE `pool.acquire()`d `&mut AnyConnection` so a user-written `BEGIN`/`COMMIT` spans them - transactions are user-owned, never auto-wrapped. The per-engine runners (`run_query_postgres`/`run_query_prepared`/`fetch_plain_text_rows`) were retyped from `&AnyPool` to `&mut AnyConnection` (use `&mut *connection` at each sqlx call to reborrow). A comment-only buffer passes the FE `canRun` guard (non-empty trimmed text) but `run_query_batch` returns an empty `Vec` with no DB round-trip - SQL tab shows neither grid nor error.
- Cancel ported verbatim from requi: `CANCEL_SENTINEL = "__cancelled__"`, `static CANCELS` registry, `CancelGuard` Drop removing the token on every exit, `cancel_query(requestId)` no-op for unknown ids, and `run_query` wraps the batch in `tokio::select! { biased; _ = token.cancelled() => Err(SENTINEL), result = run_query_batch(..) => result }`. NO server-side `pg_cancel` - cancel aborts at the next await point only (a statement already sent keeps running). FE: `SqlPane` generates a `crypto.randomUUID()` requestId per run (held in a ref), Run becomes "Cancel" while `run.isPending`, and a rejection equal to the sentinel renders a NEUTRAL muted "Cancelled" status (not red, raw sentinel never shown) and is NOT logged to History. New Cargo deps: `tokio` (sync/macros/rt-multi-thread/time) + `tokio-util` (rt).
- Toggle lag on a large open table REGRESSED a THIRD time (after the two causes in the earlier "Toggle lag" entry - remount + grid re-render were already fixed). This time it was a PERSIST FEEDBACK LOOP, not the grid. Cmd+B/Cmd+J flips `isSidebarVisible`/`isConsoleVisible` -> the WorkspaceProvider persist effect fires -> it called `persist(next)` which did `setSettings(next)` in the SettingsProvider -> SettingsProvider's context value rebuilt -> HomePage (a `useSettings` consumer) re-rendered -> the WHOLE workspace shell re-rendered, and the persist effect re-fired. On top of that, adding/removing a panel made react-resizable-panels' `onLayoutChanged` fire, and `saveLayout` did `setLayouts(...)` -> a SECOND shell re-render. Net: `Main`/`WorkspaceProvider` rendered ~8x per toggle (StrictMode-doubled), each reconciling the panel tree that holds the 200-row grid -> ~74ms. RULE for chrome/layout state that is only ever READ as an initial seed (never displayed reactively): persist it WRITE-ONLY - save to the store, do NOT `setState`. Concretely: `saveChrome(chrome)` on the settings context saves to the store and closes over the current `settings` to merge theme/shortcuts, but never `setSettings`; `layouts` moved to a `useRef` (written by `saveLayout`, read only as the `defaultLayout` seed) so `onLayoutChanged` never re-renders. Plus split the big `WorkspaceContext` into `ChromeContext` (sidebar/console) + `JsonViewContext` so a toggle only re-renders those tiny consumers, and `memo()` the `Content`/`Main` shells. Measured after: provider renders 1x/toggle (unavoidable - it flips the bool), the whole table subtree (Content/TableCard/DataGrid) = 0x, ~74ms -> ~20ms.
- How to write UI state so it can't regress perf: (1) if a piece of state is only ever consumed as an INITIAL SEED (layouts, persisted chrome flags that have their own live source of truth), keep it in a ref + write-only persist, NOT in a `setState`d context value - a reactive value there re-renders every consumer on every write for zero visual benefit AND can form a persist->setState->persist loop. (2) NEVER let a persist/save side-effect call `setState` on state that the persist effect itself depends on - that's a feedback loop. (3) Split contexts by update frequency: a rarely-changing heavy consumer (the grid) must not subscribe to a context that a frequently-toggled flag also lives in - a context value rebuild re-renders ALL consumers regardless of which field changed. (4) `memo()` the heaviest subtrees (grid, content shell) AND keep every prop referentially stable up the chain (memoize arrays, `useCallback` handlers incl. the context actions they close over) - memo is defeated by one churning prop. (5) React-compiler lint `react-hooks/refs` forbids reading/writing a ref DURING render - do ref writes in effects or event handlers only (moving `persistPayloadRef.current =`/`layoutsRef.current =` into effects is what satisfied it).
- How to MEASURE toggle/render perf (jsdom can't - it doesn't lay out or paint, so a unit test proves correctness not speed): (a) permanent regression guard - export a mutable render counter from the heavy component (`export const __dataGridRenderCount = { value: 0 }`, `+= 1` in the impl body), render the component under its REAL production wiring (SettingsProvider + the persist callback, not a bare mount - a bare mount misses the loop because `useSettingsOptional` returns null with no provider), fire the toggle, assert the counter delta is 0 (see `grid-toggle-perf.test.tsx`). (b) live in-browser measurement via chrome-devtools MCP at localhost:1431: instrument each suspect component with a temporary render tick + a `performance.now()`/rAF probe around the toggle keypress, open the app, toggle with the users table open vs closed, read the deltas, then REMOVE the instrumentation. Halve the counts you see - StrictMode double-invokes renders in dev. Guessing at the cause wasted a full round ("passed shortcuts as a prop" fixed the wrong layer); measure first.
- Adding a REQUIRED field to `DatabaseNodeBase` (`model.ts`) ripples widely: besides the persistence path (`Persisted*Database` types + a `mergeX` in `workspace.ts` + hydrate default + dehydrate omit-empty, mirroring `savedScripts`), the field must be added to (a) `newDatabaseNode` factory in `workspace-context.tsx`, (b) the field-DESTRUCTURE-and-REBUILD in `applyDatabaseConfig` (it lists every field explicitly - a rebuild that omits the new one silently drops it on any config edit), and (c) EVERY test that builds a `DatabaseNode`/`TreeNode` literal. tsc catches the typed literals, but literals cast `as unknown as TreeNode`/`DatabaseNode` (many card-test harnesses do this to skip the runtime-only fields) BYPASS tsc - they compile fine but crash at RUNTIME when the new field is read (e.g. `substituteVariables(sql, node.variables)` on `undefined`). After adding a required node field, grep `as unknown as (TreeNode|DatabaseNode)` + the node factories in test files and add the field there too, then run the FULL suite (not just tsc) - the cast literals only surface as red tests, never as type errors.
