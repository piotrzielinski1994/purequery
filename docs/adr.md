# Architectural Decisions — dbui

Append-only log of architectural and design decisions made during development.

## Format

Each entry follows this structure:

| Date | Decision | Rationale |
|------|----------|-----------|
| {YYYY-MM-DD} | {What was decided} | {Why this choice was made} |

## Decisions

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-06-19 | Desktop shell via Tauri 2 (not Electron) | Smaller bundle, native webview, Rust backend; core platform choice, expensive to swap |
| 2026-06-19 | Adopt TanStack ecosystem (Router/Query/Table/Form/Hotkeys) | Single coherent stack; permeates whole frontend architecture |
| 2026-06-19 | Mirror sibling `requi` bootstrap stack and structure | Both are minimal Tauri desktop tools; one shared mental model reduces context-switching cost |
| 2026-06-19 | Code-based TanStack Router (not file-based) | Fewer build plugins; matches requi |
| 2026-06-19 | App-wide QueryClient `retry: false` | Queries are local Tauri IPC calls, not flaky network; retry-with-backoff only delays surfacing real errors |
| 2026-06-19 | Neutral `greet` IPC in bootstrap, no DB driver bundled | Keep scaffold pure stack-proof; the DB driver choice is its own decision for a later feature |
| 2026-06-19 | Workspace UI state via one context-driven `WorkspaceProvider` + `useWorkspace` (compound components) | Every panel reads shared state (expanded folders, selection, open tabs, sub-tabs) without prop drilling; mirrors requi layout |
| 2026-06-19 | Tree + connection modeled as discriminated unions (ADT) keyed on `kind`/`type` | Exhaustive `switch`/guards over panels; no ifology; matches spec data model |
| 2026-06-19 | Results tab renders a real TanStack Table grid (not a text body) | DB-authentic result view; TanStack Table already a dependency |
| 2026-06-19 | Removed `@tanstack/react-form` + `@tanstack/react-hotkeys` deps with the bootstrap demos/palette | Nothing uses them after layout; keep dep surface honest (re-add when a real form / hotkeys land) |
| 2026-06-19 | Reworked layout: database = sidebar leaf; workbench tabs SQL/Tables/Views/Connection; statement bar removed | User feedback - matches real DB tools (TablePlus/Postico); the HTTP-ish target bar made no sense for a DB client |
| 2026-06-19 | SQL-tab editor\|results is a fixed side-by-side split, not drag-resizable | A nested react-resizable-panels group inside radix Tabs content breaks tab-switching under jsdom (see learnings); only the two shell splits are drag-resizable |
| 2026-06-19 | Layout 0.3: tables are sidebar leaves (under expandable databases) opening table cards; database card sub-tabs become SQL/Views/Script/Connection | User feedback - browse tables in the tree; Tables-as-a-tab dropped, Script added |
| 2026-06-19 | Database row has two hit targets: chevron toggles tables, name opens the card | User decision - expand and open are independent (chevron handler stops propagation) |
| 2026-06-19 | Two content-card kinds (database card / table card) resolved by `activeNode.kind` | One tab strip opens both databases and tables; the content area renders the card matching the active node's kind |
| 2026-06-19 | "workbench" + "table-cards" reworks folded back into the "layout" feature docs | User: they were iterations of layout, not separate features; one spec/plan is the source of truth |
| 2026-06-20 | DB driver = `sqlx` via `Any` pool (Postgres + MySQL), `runtime-tokio-rustls` | First real backend; this is the driver decision the bootstrap ADR deferred. `Any` lets one code path serve both engines, selected by URL scheme; rustls avoids a system OpenSSL dependency |
| 2026-06-20 | Connect is stateless: open pool -> list tables -> drop pool | No query feature needs a held connection yet (YAGNI); avoids managing a `State<Mutex<Pool>>` lifecycle. Revisit when SQL execution lands |
| 2026-06-20 | Connection settings are session-only (in-memory), not persisted | User decision; persistence (esp. plaintext passwords) is a separate security concern, out of scope here |
| 2026-06-20 | Fetched tables carry names only (empty columns/rows); engine lives on `DatabaseNode` | The feature lists tables, not their contents; opening a fetched table reuses the existing empty-grid state. Engine on the node so the Settings form seeds and the URL scheme follows it |
| 2026-06-20 | Browse table content: cast EVERY column to text in the fetch query (`::text`/`CAST AS CHAR`), capped at 200 rows | `sqlx::Any` can't decode native PG/MySQL types (uuid, timestamp, numeric, `name`); casting to text makes any column Any-decodable. 200-row cap (DBeaver default) prevents UI freeze on large tables. NULL kept via `Option<String>` -> rendered `[NULL]` |
| 2026-06-20 | Connection config stored per database id in workspace context; table content fetched via react-query keyed on table id | The table card needs the live config to query; storing it at connect-time keeps the fetch stateless backend-side while letting the frontend reuse it. react-query gives loading/error/cache for free (`retry:false` already set app-wide) |
| 2026-06-20 | Row filter = raw SQL WHERE expression, appended verbatim (not substring/parameterized) | User intent was DBeaver-style (typed `price > 10`); a substring match over text columns is the wrong tool. Raw SQL can't be a bind param, so it is the user's SQL to own (a bad expression returns a DB error). Per-column dropdown dropped as moot |
| 2026-06-20 | Cell edit writes one `UPDATE` per dirty cell, value cast to the column's catalog type (PG `$1::udt_name`), pk matched as text; pk detected server-side | Postgres won't implicitly coerce a bound text value to a typed column, so casting to the real type is required (mirror of the read-side text-cast trap). Matching the pk as text sidesteps pk-type handling. No bulk/transaction yet - acceptable for single-cell edits; revisit if multi-row atomicity is needed |
| 2026-06-20 | Settings persistence modeled on `requi`: frontend-only, `tauri-plugin-store` LazyStore -> JSON in OS app-config dir, `mergeSettings` validation gatekeeper, `SettingsStore` interface with Tauri + in-memory impls | Reuse a proven pattern from the sibling repo verbatim (same module shape, same validation/never-throw merge, same store-as-prop injection so tests never load the Tauri plugin). No backend settings logic; passwords plaintext per explicit product decision (isolated to connections.json) |
| 2026-06-20 | Two JSON files: settings.json (UI/layout) + connections.json (credentials), mirroring requi's settings.json/keymap.json split | Isolates credentials in their own file; load re-merges both through mergeSettings, save strips connections into the second file via one shared persist() helper |
| 2026-06-20 | Persistence wired into the existing WorkspaceProvider via ONE opt-in `onPersist(settings)` prop + ONE derive-and-save effect, NOT requi's granular save* context methods | dbui already owns every persisted slice in WorkspaceProvider useState; threading a save into ~8 setters is invasive and error-prone. A single effect that derives Settings from state and fires onPersist on change is one integration point, idempotent, and naturally batches. When onPersist is absent (all 13 existing tests) there is zero behavior change. Alternative (replicate requi's per-field save methods) rejected as higher blast-radius for the same result |
| 2026-06-20 | Restored connections seed connectionStatus="connected" (not idle) | The Rust backend is stateless (every fetch/query reconnects with the passed config), so a stored config is immediately usable. Seeding "connected" makes the sidebar dot correct AND the existing auto-connect effect (skips status !== undefined) leaves restored nodes alone rather than reconnecting them |
| 2026-06-20 | Persist a `layouts` slice (panel sizes per group), mirroring requi: workspace (sidebar|content), main (content|console) via react-resizable-panels `onLayoutChanged`/`defaultLayout`; sql split via a new controlled `onLeftPercentChange` callback on the hand-rolled HorizontalSplit | Initial omission left sidebar/console dimensions unsaved - a gap vs requi the user flagged. Layouts live in WorkspaceProvider state (single source of truth) and flow into the existing onPersist derive-effect, NOT a second useSettings().persist call path from the panel components (two writers of the whole Settings would clobber). The workspace group renders WITHOUT the panel group when the sidebar is hidden (requi pattern) so onLayoutChanged never overwrites the saved sidebar ratio with a single-panel {content:100} |
| 2026-06-20 | Reverse the "seed restored connections as connected" decision: restore CONFIG only via `initialConnections`; add a separate `initialConnectionStatus` prop; gate sidebar table leaves on `status === "connected"` | The earlier seed-connected choice caused a real bug (user-reported screenshot): a restored DB showed the mock tree's hardcoded tables before any real connect, because seeding "connected" made `useAutoConnect` skip (it only fires on undefined status) so the live catalog never fetched. Correct model: tables are knowable ONLY from a live `connect_database` fetch. Restore config (no re-type) -> auto-connect on open -> real catalog. Separating `initialConnectionStatus` from `initialConnections` lets persistence seed config without faking liveness, and lets tests seed a connected status explicitly |
| 2026-06-20 | Sidebar tree comes from a single persisted `workspace.json` (folders + database nodes, each node carrying its connection config); `connections.json` + the settings `connections` slice REMOVED | Supersedes the 2026-06-20 "two JSON files: settings.json + connections.json" decision. connections.json was redundant - a DatabaseNode already carries engine/host/port/database/user/password, so the parallel store duplicated node fields and the sidebar still showed fixed mock data. requi-faithful split: structure/connections in workspace.json (loaded via WorkspaceStoreProvider -> hydrate -> WorkspaceProvider), UI state in settings.json. hydrate/dehydrate bridge persisted shape <-> runtime TreeNode (runtime fields tables/views/sql/result NEVER persist - tables come from a live connect). Single manifest, NOT requi's directory-scan (YAGNI for a DB client). Empty start (no mock) when the file is absent. Add/Remove/Rename-database UI is the next slice |
| 2026-06-21 | Third engine `DbEngine::Sqlite` via sqlx `sqlite` feature; `run_query` routes SQLite through the MySQL `prepare().columns()` path (renamed `run_query_mysql` -> `run_query_prepared(engine)`) | SQLite's `Any` type-describe has no Postgres-style exotic-type failure, so the prepared path works as-is; sharing avoids a third copy. Falls back to a json-wrap only if a live test shows a describe failure (unverified in CI - no live DB). SQLite quoting == Postgres double-quote; text cast = `CAST(.. AS TEXT)` |
| 2026-06-21 | `ConnectionConfig` becomes an engine-tagged discriminated union (network variant host/port/database/user/password for postgres/mysql; sqlite variant `{ file }`); Rust side `#[serde(tag="engine")]` enum; one `connectionOf(node)` helper centralises extraction | First engine that does NOT fit the flat host/port/user/password shape. Partially reverses the implicit "DatabaseNode carries flat connection fields" model (see the 2026-06-19 learnings entry that dropped the HTTP auth union): a true ADT makes SQLite-with-a-host unrepresentable, matching the "tree+connection as discriminated unions" precedent. Accepted higher churn (5 prod + ~11 test readers of flat fields) bounded via `connectionOf` + a shared `DatabaseNodeBase`. Per-engine query fns keep taking `DbEngine` via `config.engine()` so the ~13 query builders are untouched |
| 2026-06-21 | REVERSE "Connect is stateless": hold a pool per database id in a process-wide `static LazyLock<Mutex<HashMap<id, HeldConnection>>>` (pool + engine), opened on connect, closed on disconnect; commands address a connection by id, `config` sent only on connect | The 2026-06-20 stateless ADR explicitly said "revisit when SQL execution lands" - it has (transactions + cancel need a held connection). Chose a `static` registry over a Tauri-managed `State<>` to mirror requi's cancel-registry shape and keep `db.rs` command fns as free functions (no `State` param threaded through ~8 commands). `AnyPool` is a cheap `Arc` clone + `DbEngine` is `Copy`, so `with_pool(id)` clones the handle out of the lock and never holds the mutex across an `.await`. Engine travels in the registry because commands no longer carry `config`. Churn: every command gained a `connection_id` first arg, ~10 FE test suites re-pointed from `config` to the id |
| 2026-06-21 | Multi-statement runs are user-owned transactions, NOT auto-wrapped; `execute_sql` returns `Vec<QueryOutcome>` (one per statement) run in order on ONE acquired connection | A DB client must let users write their own `BEGIN`/`COMMIT`/`ROLLBACK`/`SET`; auto-wrapping every batch in a transaction would change semantics (DDL, VACUUM) and surprise them. The only requirement is same-connection execution (`pool.acquire()` once, loop), which a held pool enables. A failing statement stops the batch; prior statements stay applied unless the user wrapped them. Per-engine classify/wrap/execute moved from `&AnyPool` to `&mut AnyConnection` so the whole batch shares the connection |
| 2026-06-21 | Cancel aborts at the next await point via `tokio::select!` against a per-request `CancellationToken` (ported from requi), NOT a server-side `pg_cancel`/`KILL QUERY` | Matches requi's proven pattern (registry + `CancelGuard` Drop + `CANCEL_SENTINEL`) and is enough for the UI affordance (Run becomes Cancel). A true server-side kill needs a SECOND connection issuing `pg_cancel_backend`/`KILL QUERY` - deferred as YAGNI for v1; a statement already sent to the DB keeps running until it yields. Documented limitation, not a bug |
| 2026-06-26 | Postgres-only schema level in the sidebar (MySQL "schema"==database, SQLite none stay flat), addressed end-to-end as `(schema, table)` - NOT display-only grouping | A Postgres DB has many schemas; the flat catalog collapsed `public.users` and `analytics.users` into one ambiguous leaf and every table command hit whichever the server `search_path` resolved first - a silent wrong-table bug. Full qualified addressing (catalog returns `TableRef{schema,name}`; `fetch_table`/`count_table`/`apply_mutations` take `schema: Option<String>`; PG introspection pinned to `table_schema = $schema`, not search_path) makes same-named tables independent. Display-only grouping was rejected: it leaves the addressing bug intact. Uniform schema level + cross-DB browse rejected as artificial / bigger scope |
| 2026-06-26 | Schema is RENDER-DERIVED on `TableNode.schema: string\|null` (not a persisted `SchemaNode`), AND rendered FLAT as a leaf-label qualifier - NOT a nested expandable schema row | Two-part. (a) Render-derived: keeps the data model `Database > Table` (flat list with a tag) so `indexNodes`/`indexTableParents`/`replaceDatabaseTables`/the grid path are untouched. (b) Flat label - REVERSES the same-day initial nested `Connection > Schema > Table` tree after user feedback that the extra expand level was noise: a multi-schema Postgres database shows `schema.table` leaves directly under the connection; single-schema (and MySQL/SQLite) show bare names. `isMultiSchema(tables)` decides; no synthetic schema-row id, no extra `expandedIds` entry. Backend qualified addressing unchanged - sidebar-rendering only. Same change fixed SQL autocomplete to a nested `schema->table->columns` namespace + `defaultSchema=public` (the prior name-keyed flat map let `vehicle_listing.users` clobber `public.users`, suggesting an unqualified table that failed to resolve) |
| 2026-06-26 | Multi-theme = appearance MODE (light/dark/system) + per-mode SPARSE color overrides, persisted SPLIT across `settings.json` (mode) and a separate `theme.json` (colors, key "colors") - mirroring sibling `requi` | A DB client wants OS-following appearance + the ability to tweak individual tokens. Stored sparse (only diffs from the built-in `DEFAULT_THEME_COLORS`) so an un-customized token tracks the default and a token reset-to-default drops out. The settings.json/theme.json SPLIT (colors stripped from the settings.json payload on save, recombined on load) makes a color scheme device-syncable on its own, matching requi's keymap-split shape. Effective mode toggles a `.dark` class on `<html>`; overrides apply as inline `--<token>` CSS vars (app tokens only - editor-syntax tokens flow through CodeMirror extensions, not the DOM). The SQL editor's chrome+highlight became theme-token-driven factories memoized on color VALUES+mode so it reconfigures-in-place (document preserved), never remounts. Color editor is a raw-JSON CodeMirror pane validated by a zod-v4-derived JSON schema (`z.toJSONSchema`); NOT requi's full `registerActiveEditor` active-editor seam (dbui has no request/config/tab-close consumers) - it saves self-contained via a Save button + Mod+S keymap. New deps: zod v4, @codemirror/lang-json, @codemirror/lint, codemirror-json-schema (inlined in vitest for ESM resolution) |
| 2026-06-27 | MongoDB (first DOCUMENT engine) gets a SEPARATE `src-tauri/src/mongo.rs` module + the official `mongodb` crate, NOT a `db.rs`/`sqlx::Any` arm; `lib.rs` is a dispatcher that routes each connection-addressed command to the Mongo path when `mongo::is_connected(id)` and to the SQL path otherwise | `sqlx::Any` has no Mongo support and Mongo is not SQL, so a fourth `DbEngine` arm was impossible. Isolating it keeps `db.rs` + all 100+ SQL tests untouched and creates a reusable non-SQL data-source seam (any future Redis/Dynamo reuses it). `connect_database` now takes a raw `serde_json::Value` and peeks the `engine` tag to pick the deserializer (`ConnectionConfig` SQL enum vs `MongoConfig`), because `mongodb` is deliberately NOT a variant of the SQL `ConnectionConfig` enum. Mongo connect reuses `db.rs`'s cancel registry via new `register/unregister_cancel_token` so the Settings Cancel button aborts a stuck Mongo connect identically |
| 2026-06-28 | Mongo Query tab REUSES the SQL editor pane (saved-script tabs / Run / Cancel / History); the JSON `db.<coll>.find({...})` / `aggregate([...])` command is self-contained (collection in the text), NOT a collection-picker + Find/Aggregate combobox | User required SQL parity - same saveable script tabs across engines. A bespoke picker forked the editor UX and made queries unsaveable (collection lived outside the text). Backend grew a tiny `parse_command` (`db.<coll>.<find\|aggregate>(<json>)`) + `split_commands` (top-level `;`, bracket/quote aware) and a SQL-shaped `run_query -> Vec<QueryOutcome>` (cancellable via the shared registry); `execute_mongo_find`/`_aggregate` collapsed into one `execute_mongo`. Frontend: `SqlTab` picks `executeMongo` vs `executeSql` by `node.engine`; the dedicated `mongo-query-tab.tsx` was deleted. Trade-off: a hand-rolled command parser (not full mongosh) - only find/aggregate, no method chaining/options - accepted as enough for the JSON-query scope |
| 2026-06-28 | MongoDB Extended JSON type wrappers (`$oid`/`$numberLong`/`$numberInt`/`$numberDouble`/`$date`) decoded in filters + pipelines so an ObjectId `_id` is queryable as `{"_id":{"$oid":"..."}}` | A plain-JSON filter could never match an ObjectId `_id` (string != ObjectId) - the seeded `users` collection surfaced this immediately. A single-key object whose key is a known `$`-wrapper decodes to the BSON type; query operators (`$gt`) and multi-key objects stay plain documents. A malformed wrapper (bad `$oid` hex) is a clear filter error, not a silent literal. Rejected auto-coercing any 24-hex string (magic, can corrupt a legit hex string `_id`) |
| 2026-06-27 | The Mongo path REUSES the shared IPC structs (`TableRef`/`TableRows`/`TableColumn`/`QueryOutcome`/`RowMutation`) and the ONE `DataGrid`; documents are flattened DBeaver-style (top-level keys -> columns, `_id` first + primary key, nested object/array -> compact JSON text in-cell, scalar -> JSON-literal text, missing key -> NULL cell) | Honors the non-negotiable ONE-DataGrid invariant (CLAUDE.md) so the frontend grid/paging/sort/count/Save pipeline stays engine-agnostic - the table card renders a collection exactly like a SQL table. Forking a second document grid was rejected. A new shared `RowMutation::Replace { pk_value, document }` variant carries the full-document edit; the SQL `build_mutation` rejects it (`Err`), the Mongo path maps cell/insert/delete/replace to updateOne-$set/insertOne/deleteOne/replaceOne. Cell + insert values are parsed as JSON literals (number/bool/null/string) so BSON scalar types survive an edit; an `_id` that parses as an ObjectId matches as one, else by its raw value. The crate-wide serde_json `arbitrary_precision` feature forced building BSON via a hand-rolled `Value -> Bson` map instead of `bson::to_document` (which emits a `$serde_json::private::Number` wrapper Mongo rejects). Per-engine UI differences (History preview strings, filter syntax) live behind one `queryPreview(engine)` strategy + a `mongodb` JSON CodeMirror mode, not `engine===` branches in the table card. Mongo card shows Query (find/aggregate JSON) + Settings only; no SQL/Views/Script. New Rust deps: `mongodb`, `futures-util` |
| 2026-07-09 | F13 FK navigation gets an explicit back/forward history (a `(tableId, filter)` stack + cursor in `WorkspaceProvider`, pure reducer in `nav-history.ts`, Back/Forward buttons + `Mod+[`/`Mod+]`), REVERSING the plan's original "no stack - rely on the tab bar as the visit trail" | The tab-bar-as-trail choice was made for minimal diff, but the user rejected it after using the feature: after a chain of FK jumps the tab bar shows every target as a peer tab with no notion of "where I came from", so returning is a hunt, not a Back. A browser-style linear history (push on jump, drop-forward-on-new-jump, restore tab+filter on back/forward) matches the mental model of drilling through relations. Kept in-memory only (not persisted) - navigation context is ephemeral like `tableFilters`. The pure reducer isolates the stack semantics from the provider so it is unit-tested without a webview |
| 2026-07-09 | The Postgres FK introspection query reads the referenced side from `key_column_usage` correlated by `position_in_unique_constraint`, NOT from `information_schema.constraint_column_usage` | The original F13 query joined `constraint_column_usage` on constraint_name alone, which for a COMPOSITE FK is a cartesian product (a 2-column FK yields 4 rows: every local column paired with every referenced column), so the folded FK carried duplicated columns and the "Go to" label read `region=EU, region=EU, code=W1, code=W1`. Correlating by position yields exactly one referenced column per local column in order. A single-column FK never exposed this (1x1=1). The unit test that "verified" fold correctness used hand-crafted correct tuples, so it missed the query bug - the fix pins the query text itself (must not contain `constraint_column_usage`, must correlate by `position_in_unique_constraint`) |
| 2026-07-10 | F18 Session Logs tab feeds off tauri-plugin-log's `TargetKind::Webview` (a `log://log` event the FE reads via `attachLogger`), NOT a per-dispatcher `app.emit` at each `lib.rs` log call site | The alternative - emitting a second event next to each existing `log::info!/error!` - would duplicate the emit at ~6 sites, fight the standing "one call site per command" logging rule, and be silently forgotten on every new command. The Webview target is a single additive line in `logging::init` that forwards EVERY record (backend + the FE `log_message` bridge) with zero call-site churn, and the file target is untouched (each line still written once). Cost: one JS dep (`@tauri-apps/plugin-log`) + accepting the plugin's own line formatting. A "read the file on open + Refresh" design was rejected (no live tail; needs a new read-back command); live-only push is the chosen trade-off, with the documented gap that pre-mount startup lines (`dbui starting`, launch auto-connect) are not shown |
| 2026-07-10 | F12 commit/rollback confirmation is a FRONTEND confirm-before-send gate (per-database `confirmWrites` flag, dialog shows the exact SQL, Commit sends / Cancel discards), NOT a real backend manual-commit transaction | The todos.md F12 title is "confirmation layer" and its dominant phrasing is "write NIE leci od razu - trafia do warstwy potwierdzenia z widocznym SQL i przyciskami Commit/Rollback" (held-then-confirmed, not executed) and "Pairuje z F11" - F11 (read-only) is a no-backend-change frontend boundary gate. A true DBeaver manual-commit (BEGIN on a pinned connection, hold across the session, COMMIT/ROLLBACK; Mongo sessions/transactions) needs backend transaction-state on the held pool (only 5 connections, one pinned per manual-commit DB) and has NO requi/vidui precedent to mirror. Chose the frontend gate: mirrors F11 byte-for-byte (persisted `confirmWrites` on the node, `mergeConfirmWrites`, dialog gate on the same two write paths - table Save + write-shaped SQL Run), zero backend risk, safe to complete unattended. `readOnly` takes precedence (a read-only DB blocks outright, never shows the Commit dialog). The real backend manual-commit remains a separate, larger future feature |
| 2026-07-11 | REVERSE the 2026-07-10 F12 confirm-gate decision: F12 becomes REAL manual-commit transaction mode (auto-commit OFF), not a frontend confirm-before-send dialog | The user clarified against a DataGrip/DBeaver screenshot that the ticket wants real manual-commit - writes hit the DB immediately but inside an open transaction, a SELECT sees the uncommitted rows, and a Commit/Rollback toolbar finishes the tx at the DB level. The confirm-gate (hold the SQL, send on Commit, nothing sent on Cancel) was the wrong interpretation. Real design: a per-connection transaction-session registry (Unit of Work) pins a `PoolConnection` from the held pool and issues literal `BEGIN` on the first write; while open, EVERY connection-addressed SQL command (reads + writes) routes through that pinned connection (so uncommitted rows are visible) instead of a fresh pool connection - the bulk of the change is refactoring db.rs read/mutation helpers from `&AnyPool` to `&mut AnyConnection`. `commit_transaction`/`rollback_transaction`/`begin_transaction`/`transaction_state` are new commands; disconnect force-rolls-back to avoid leaking the pinned connection. SQL-only (Postgres/MySQL/SQLite) - MongoDB excluded, mirroring DBeaver's NoSQL driver having no manual-commit toolbar (Mongo tx needs a replica set the standalone test-stack lacks). `BEGIN`-on-first-write (not on entering the mode) keeps pure browsing on the pool so no idle-in-transaction connection is held. This supersedes the frontend `confirmWrites` flag, which is replaced by `manualCommit` on the node |
| 2026-07-12 | Manual-commit (F12) wraps each statement/mutation in a per-statement `SAVEPOINT` inside the open transaction, so a failed statement does not poison the whole tx | On Postgres a failed statement marks the entire transaction aborted ("current transaction is aborted, commands ignored until end of transaction block") - every later command fails until ROLLBACK, so a user who hit one bad write was stuck. DBeaver avoids this with "use savepoint for each statement". dbui now does the same: inside the pinned tx connection `run_one_in_savepoint`/`execute_mutation_in_savepoint` issue `SAVEPOINT dbui_stmt` before each statement, `RELEASE` on success, `ROLLBACK TO SAVEPOINT` on failure (undoing only that statement, keeping the tx usable). Gated on `ConnHandle::is_pinned` - the pooled/auto-commit path takes no savepoint (a savepoint outside a transaction is an error). The batch still stops at the first error (unchanged contract), but the NEXT Run works. Verified red-green on real Postgres (`live_pg_savepoint_keeps_tx_usable_after_a_failed_statement`): without the savepoint the recovery insert returns the aborted-tx error; with it, it succeeds and commits. SQLite does not abort a tx on statement error, so its in-process test is only a savepoint-SQL smoke. |
